---
phase: 00-foundation-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "00-01"
files_modified:
  # Backend entities, migrations
  - achievement-module/achievement-system/src/main/resources/db/migration/V1__init_system_tables.sql
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysUser.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysRole.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysDepartment.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysDictCategory.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysDictEntry.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysMenu.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysUserRole.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysRoleMenu.java
  # Backend mappers
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/mapper/SysUserMapper.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/mapper/SysRoleMapper.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/mapper/SysDepartmentMapper.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/mapper/SysDictCategoryMapper.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/mapper/SysDictEntryMapper.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/mapper/SysMenuMapper.java
  # Backend DTOs
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/UserCreateDTO.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/UserUpdateDTO.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/UserPageDTO.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/RoleCreateDTO.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/RoleUpdateDTO.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/DepartmentCreateDTO.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/DepartmentUpdateDTO.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/DictCategoryDTO.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/DictEntryDTO.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/MenuTreeNode.java
  # Backend controllers
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/UserController.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/RoleController.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/DepartmentController.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/DictCategoryController.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/DictEntryController.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/MenuController.java
  # Backend services
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/UserService.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/impl/UserServiceImpl.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/RoleService.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/impl/RoleServiceImpl.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/DepartmentService.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/impl/DepartmentServiceImpl.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/DictCategoryService.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/impl/DictCategoryServiceImpl.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/DictEntryService.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/impl/DictEntryServiceImpl.java
  # Frontend views (P-03, P-04, P-05, P-06)
  - achievement-web/src/views/system/user/index.vue
  - achievement-web/src/views/system/role/index.vue
  - achievement-web/src/views/system/department/index.vue
  - achievement-web/src/views/system/dict/index.vue
  - achievement-web/src/api/system/user.ts
  - achievement-web/src/api/system/role.ts
  - achievement-web/src/api/system/department.ts
  - achievement-web/src/api/system/dict.ts
  - achievement-web/src/router/index.ts
autonomous: true
requirements:
  - SYS-01
  - SYS-02
  - SYS-05
  - SYS-08

must_haves:
  truths:
    - "System admin can create/edit/delete users with role assignment and department selection via drawer form"
    - "System admin can create/edit/delete roles with tree-based menu permission assignment via drawer"
    - "System admin can create/edit/delete departments (flat structure, no hierarchy)"
    - "System admin can manage data dictionary categories and entries via left-tree-right-table interface"
    - "System admin can import users via CSV upload (match by employee ID, update existing, insert new)"
    - "Users have enable/disable status control; disabled users cannot log in"
    - "Soft delete preserves user data, marks as deleted"
    - "Password follows policy: minimum 8 characters, must contain letter + number"
  artifacts:
    - path: "achievement-module/achievement-system/src/main/resources/db/migration/V1__init_system_tables.sql"
      provides: "FLYWAY migration creating all Phase 0 table: sys_user, sys_role, sys_department, sys_dict_category, sys_dict_entry, sys_menu, sys_user_role, sys_role_menu"
      min_lines: 80
    - path: "achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/UserController.java"
      provides: "REST API for user CRUD + CSV import + enable/disable + reset password"
      contains: "@RestController"
    - path: "achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/RoleController.java"
      provides: "REST API for role CRUD + permission assignment"
      contains: "@RestController"
    - path: "achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/DictCategoryController.java"
      provides: "REST API for dict category CRUD (tree operations)"
      contains: "@RestController"
    - path: "achievement-web/src/views/system/user/index.vue"
      provides: "P-03 User Management page: ElTable + ElDrawer editing + CSV import dialog + search form"
      min_lines: 300
    - path: "achievement-web/src/views/system/role/index.vue"
      provides: "P-04 Role Management page: ElTable + ElDrawer with ElTree permission tree"
      min_lines: 200
    - path: "achievement-web/src/views/system/dict/index.vue"
      provides: "P-06 Data Dictionary page: left ElTree categories + right ElTable entries"
      min_lines: 200
  key_links:
    - from: "UserController.java"
      to: "SysUserMapper.java"
      via: "MyBatis-Plus BaseMapper injection in service impl"
      pattern: "BaseMapper<SysUser>"
    - from: "UserServiceImpl.java"
      to: "SysUser.java"
      via: "SAVE and UPDATE operations with password encoding and logic-delete support"
      pattern: "logicDeleteById|insert|updateById"
    - from: "RoleController.java"
      to: "SysRoleMapper.java / SysRoleMenuMapper.java"
      via: "Role CRUD + batch menu permission operations"
      pattern: "saveBatch|removeById"
    - from: "achievement-web/src/views/system/user/index.vue"
      to: "achievement-web/src/api/system/user.ts"
      via: "API service import for list/create/update/delete/import calls"
      pattern: "import.*api.*user"
---

<objective>
Implement the complete user management, role management with RBAC menu permissions, department management, and data dictionary system -- both backend and frontend.

Purpose: Deliver the core system administration capability. System admins can manage users (7 roles per SYS-01), roles with tree-based permission assignment (SYS-02), flat departments with data isolation (D-08), CSV import for personnel (SYS-05 / D-09), and data dictionary management (SYS-08 / D-10).

Output: Running CRUD APIs and 4 fully functional system management pages (P-03 User Management, P-04 Role Management, P-05 Department Management, P-06 Data Dictionary).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation-infrastructure/00-CONTEXT.md (D-06 through D-20: user/RBAC decisions)
@.planning/phases/00-foundation-infrastructure/00-UI-SPEC.md (P-03, P-04, P-05, P-06 full contracts)
@.planning/REQUIREMENTS.md (SYS-01, SYS-02, SYS-05, SYS-08)
@CLAUDE.md (MyBatis-Plus, MapStruct, EasyExcel)

<interfaces>
Contract created by Plan 00-01 (this plan builds on it):
- Backend module: achievement-framework (config classes + common utilities)
- Backend module: achievement-system (application entry point)
- Frontend scaffold: achievement-web with router, Pinia stores, Axios instance
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Flyway Migration V1 + All Entities + MyBatis-Plus Mappers</name>
  <files>
    achievement-module/achievement-system/src/main/resources/db/migration/V1__init_system_tables.sql,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysUser.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysRole.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysDepartment.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysMenu.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysDictCategory.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysDictEntry.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysUserRole.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysRoleMenu.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/mapper/SysUserMapper.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/mapper/SysRoleMapper.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/mapper/SysDepartmentMapper.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/mapper/SysMenuMapper.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/mapper/SysDictCategoryMapper.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/mapper/SysDictEntryMapper.java
  </files>
  <read_first>
    CONTEXT.md D-06 through D-20 (user management decisions)
    D-08: flat departments, no hierarchy
    D-12: password policy 8+letter+number
    D-15: 5 login failures, 30-minute lockout (lockout fields on sys_user)
    D-17: multi-role support (sys_user_role join table)
    D-19: enable/disable status (status field)
    D-20: soft delete (deleted field)
    UI-SPEC.md P-03 table columns, P-04 table columns, P-05 table columns, P-06 table columns
  </read_first>
  <action>
    Create Flyway V1 migration SQL file and corresponding Java entities + MyBatis-Plus mappers.

    1. Create `V1__init_system_tables.sql` with these tables:

    **sys_department** (D-08 flat structure, no parent_id):
    - id BIGINT AUTO_INCREMENT PRIMARY KEY
    - dept_name VARCHAR(100) NOT NULL
    - dept_code VARCHAR(50) NOT NULL UNIQUE
    - leader VARCHAR(100) (nullable, department负责人)
    - phone VARCHAR(20) (nullable)
    - status TINYINT DEFAULT 1 (1=正常, 0=停用)
    - deleted TINYINT DEFAULT 0 (soft delete per D-20)
    - created_at DATETIME, created_by VARCHAR(50)
    - updated_at DATETIME, updated_by VARCHAR(50)

    **sys_menu** (menu tree for RBAC -- menus/route identifiers):
    - id BIGINT AUTO_INCREMENT PRIMARY KEY
    - parent_id BIGINT DEFAULT 0 (0 = root, self-referencing for tree)
    - name VARCHAR(100) NOT NULL (menu display name)
    - permission VARCHAR(200) (permission identifier like "system:user:list")
    - path VARCHAR(200) (route path, nullable for parent menus)
    - component VARCHAR(200) (Vue component path)
    - type TINYINT NOT NULL (0=directory, 1=menu, 2=button)
    - icon VARCHAR(100) (Element Plus icon name)
    - sort_order INT DEFAULT 0
    - status TINYINT DEFAULT 1
    - created_at DATETIME, updated_at DATETIME

    **sys_role** (7 roles per SYS-01):
    - id BIGINT AUTO_INCREMENT PRIMARY KEY
    - role_name VARCHAR(50) NOT NULL (e.g., "系统管理员")
    - role_code VARCHAR(50) NOT NULL UNIQUE (e.g., "ROLE_ADMIN")
    - description VARCHAR(500)
    - status TINYINT DEFAULT 1
    - deleted TINYINT DEFAULT 0
    - created_at DATETIME, created_by VARCHAR(50)
    - updated_at DATETIME, updated_by VARCHAR(50)

    **sys_user** (self-contained account per D-06/D-19/D-20):
    - id BIGINT AUTO_INCREMENT PRIMARY KEY
    - username VARCHAR(50) NOT NULL UNIQUE
    - password VARCHAR(255) NOT NULL (BCrypt hash)
    - real_name VARCHAR(100)
    - email VARCHAR(100)
    - phone VARCHAR(20)
    - dept_id BIGINT (FK to sys_department.id, nullable for 未分配)
    - status TINYINT DEFAULT 1 (1=正常, 0=停用 per D-19)
    - deleted TINYINT DEFAULT 0 (logic delete per D-20)
    - lockout_until DATETIME (nullable, account lockout per D-15)
    - login_failures INT DEFAULT 0 (consecutive failures per D-15)
    - last_login_ip VARCHAR(50)
    - last_login_time DATETIME
    - password_change_required TINYINT DEFAULT 1 (1=force change on first login per D-13)
    - created_at DATETIME, created_by VARCHAR(50)
    - updated_at DATETIME, updated_by VARCHAR(50)

    **sys_user_role** (multi-role per D-17):
    - id BIGINT AUTO_INCREMENT PRIMARY KEY
    - user_id BIGINT NOT NULL (FK)
    - role_id BIGINT NOT NULL (FK)
    - UNIQUE KEY uk_user_role (user_id, role_id)

    **sys_role_menu** (RBAC menu permission mapping):
    - id BIGINT AUTO_INCREMENT PRIMARY KEY
    - role_id BIGINT NOT NULL (FK)
    - menu_id BIGINT NOT NULL (FK)
    - UNIQUE KEY uk_role_menu (role_id, menu_id)

    **sys_dict_category** (D-10 left tree):
    - id BIGINT AUTO_INCREMENT PRIMARY KEY
    - category_name VARCHAR(100) NOT NULL
    - category_code VARCHAR(50) NOT NULL UNIQUE
    - description VARCHAR(500)
    - sort_order INT DEFAULT 0
    - status TINYINT DEFAULT 1
    - created_at DATETIME, created_by VARCHAR(50)
    - updated_at DATETIME, updated_by VARCHAR(50)

    **sys_dict_entry** (D-10 right table):
    - id BIGINT AUTO_INCREMENT PRIMARY KEY
    - category_id BIGINT NOT NULL (FK to sys_dict_category.id)
    - dict_key VARCHAR(100) NOT NULL
    - dict_value VARCHAR(200) NOT NULL
    - sort_order INT DEFAULT 0
    - status TINYINT DEFAULT 1
    - created_at DATETIME, created_by VARCHAR(50)
    - updated_at DATETIME, updated_by VARCHAR(50)
    - UNIQUE KEY uk_category_key (category_id, dict_key)

    Add INDEX on: sys_user(dept_id), sys_user(username), sys_dict_entry(category_id), sys_menu(parent_id)

    Seed data in the same migration:
    - Insert default admin user: username='admin', password=BCrypt('admin123'), real_name='系统管理员', password_change_required=1
    - Insert 7 default roles per SYS-01: 科研人员/科研秘书/部门管理员/主管院领导/涉密成果管理员/内审审计人员/系统管理员
    - Insert base menu tree matching P-10: 首页 (/dashboard), 系统管理 parent -> 6 children routes
    - Assign all menus to 系统管理员 role
    - Insert base dict categories: 成果类型, 专利类型, 收录情况, 经费来源, 预警等级

    2. Create Java entity classes in `com.institute.achievement.module.system.entity`:
       - Each entity uses Lombok @Data, @TableName("sys_xxx"), @TableId, @TableField
       - SysUser: extends Model<SysUser>, has @TableLogic annotation on deleted field
       - SysRole: @TableLogic on deleted
       - SysDepartment: @TableLogic on deleted
       - SysDictCategory, SysDictEntry: basic entities
       - SysUserRole, SysRoleMenu: join table entities
       - All entities use Long for ID, LocalDateTime for date fields
       - Use `@TableField(fill = FieldFill.INSERT)` for created_at/created_by, `@TableField(fill = FieldFill.INSERT_UPDATE)` for updated_at/updated_by
       - Create a `MetaObjectHandler` implementation in `achievement-framework/src/main/java/com/institute/achievement/framework/config/MyBatisPlusMetaHandler.java` that auto-fills created_at/updated_at with LocalDateTime.now() and created_by/updated_by with current username from SecurityContextHolder

    3. Create MyBatis-Plus mapper interfaces in `com.institute.achievement.module.system.mapper`:
       - Each extends `BaseMapper<Entity>`
       - SysUserMapper: no custom methods (BaseMapper provides CRUD + logic delete)
       - SysRoleMapper: add `@Select("SELECT sr.* FROM sys_role sr JOIN sys_user_role sur ON sr.id = sur.role_id WHERE sur.user_id = #{userId}") List<SysRole> selectRolesByUserId(Long userId)`
       - SysMenuMapper: add `@Select("SELECT sm.* FROM sys_menu sm JOIN sys_role_menu srm ON sm.id = srm.menu_id WHERE srm.role_id = #{roleId} ORDER BY sm.sort_order") List<SysMenu> selectMenusByRoleId(Long roleId)` and `List<SysMenu> selectMenusByUserId(Long userId)` (via nested join)
       - SysDictEntryMapper: add `@Select("SELECT * FROM sys_dict_entry WHERE category_id = #{categoryId} AND status = 1 ORDER BY sort_order") List<SysDictEntry> selectByCategoryId(Long categoryId)`
       - SysDictCategoryMapper: add `List<SysDictCategory> selectAllOrderBySort()`
       - SysDepartmentMapper: no custom methods

    Write all files via the Write tool.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE" achievement-module/achievement-system/src/main/resources/db/migration/V1__init_system_tables.sql && grep -c "@TableName" achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/entity/SysUser.java</automated>
  </verify>
  <acceptance_criteria>
    1. V1 migration creates 8 tables: sys_user, sys_role, sys_department, sys_menu, sys_dict_category, sys_dict_entry, sys_user_role, sys_role_menu
    2. sys_user has fields: username, password, real_name, email, phone, dept_id, status, deleted, lockout_until, login_failures, last_login_ip, last_login_time, password_change_required, auth_source VARCHAR(20) DEFAULT 'LOCAL', external_ref_id VARCHAR(200) NULL
    3. sys_user has @TableLogic on deleted field
    4. Seed data inserts admin user with BCrypt('admin123'), password_change_required=1 per D-13
    5. Seed data inserts 7 roles with codes: ROLE_RESEARCHER, ROLE_SECRETARY, ROLE_DEPT_ADMIN, ROLE_LEADER, ROLE_CLASSIFIED, ROLE_AUDITOR, ROLE_SYSTEM_ADMIN
    6. Seed data inserts menu tree with 系统管理 parent + 6 children per D-36
    7. Seed menu items have permission identifiers (e.g., "system:user:list")
    8. MetaObjectHandler fills created_at/created_by/updated_at/updated_by automatically
    9. `./mvnw compile -pl achievement-module/achievement-system -am` succeeds
  </acceptance_criteria>
  <done>Flyway V1 migration creates all 8 system tables with seed data (default admin, 7 roles, menu tree, base dict categories). All Java entities with MyBatis-Plus annotations and mappers compile. MetaObjectHandler auto-fills audit fields.</done>
</task>

<task type="auto">
  <name>Task 2: Create Backend CRUD Services + Controllers + DTOs + CSV Import</name>
  <files>
    # DTOs
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/UserCreateDTO.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/UserUpdateDTO.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/UserPageDTO.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/UserVO.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/RoleCreateDTO.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/RoleUpdateDTO.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/RoleVO.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/DepartmentCreateDTO.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/DepartmentUpdateDTO.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/DepartmentVO.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/DictCategoryDTO.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/DictEntryDTO.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/dto/MenuTreeNode.java,
    # Services
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/UserService.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/impl/UserServiceImpl.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/RoleService.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/impl/RoleServiceImpl.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/DepartmentService.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/impl/DepartmentServiceImpl.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/DictCategoryService.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/impl/DictCategoryServiceImpl.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/DictEntryService.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/impl/DictEntryServiceImpl.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/MenuService.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/service/impl/MenuServiceImpl.java,
    # Controllers
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/UserController.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/RoleController.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/DepartmentController.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/DictCategoryController.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/DictEntryController.java,
    achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/controller/MenuController.java
  </files>
  <read_first>
    CONTEXT.md: D-09 (CSV import overwrite strategy -- match by employee ID), D-11 (role multi-select), D-14 (password reset by admin), D-15 (account lockout), D-18 (tree permission assignment)
    UI-SPEC.md: P-03 drawer editing fields, P-04 drawer with ElTree permission assignment, P-05 drawer editing, P-06 drawer editing
    Task 1 output: entity field names for DTO mapping logic
    CLAUDE.md: EasyExcel 4.0+ for CSV import
  </read_first>
  <action>
    Create all backend DTOs, service interfaces/implementations, and controllers.

    1. Create DTO classes:
       - `UserCreateDTO`: username (NotBlank, 4-50), password (NotBlank, min 8, must match pattern for letter+number validation -- can validate in service), realName, email (Email), phone, deptId, roleIds (List&lt;Long&gt;)
       - `UserUpdateDTO`: id, realName, email, phone, deptId, roleIds (List&lt;Long&gt;), status
       - `UserPageDTO`: keyword, deptId, roleId, status, page, pageSize (extends PageQuery)
       - `UserVO`: id, username, realName, email, phone, deptId, deptName, roleIds, roleNames (List&lt;String&gt;), status, lastLoginIp, lastLoginTime, createdAt (VO hides password, lockout fields, deleted flag)
       - `RoleCreateDTO`: roleName (NotBlank), roleCode (NotBlank, unique), description
       - `RoleUpdateDTO`: id, roleName, description, status
       - `RoleVO`: id, roleName, roleCode, description, status, userCount, createdAt
       - `DepartmentCreateDTO`: deptName (NotBlank), deptCode (NotBlank, unique), leader, phone, status
       - `DepartmentUpdateDTO`: id, deptName, deptCode, leader, phone, status
       - `DepartmentVO`: id, deptName, deptCode, leader, phone, status, memberCount, createdAt
       - `DictCategoryDTO`: id, categoryName, categoryCode, description, sortOrder, status
       - `DictEntryDTO`: id, categoryId, categoryName, dictKey, dictValue, sortOrder, status
       - `MenuTreeNode`: id, parentId, label (menu name), permission, type, icon, sortOrder, children (List&lt;MenuTreeNode&gt;), checked (boolean, for role permission assignment)

       All DTOs use Lombok @Data. Use jakarta.validation.constraints for validation annotations.

    2. Create service interfaces and implementations:

       **UserService / UserServiceImpl:**
       - `PageResult&lt;UserVO&gt; page(UserPageDTO dto)` -- MyBatis-Plus Page with wrapper conditions: like username/realName when keyword present, eq deptId, eq status. Exclude deleted=1.
       - `UserVO getById(Long id)` -- wrap entity to VO
       - `void create(UserCreateDTO dto)` -- create with BCryptPasswordEncoder, check username unique, insert user_role associations
       - `void update(UserUpdateDTO dto)` -- update fields, update user_role (remove old, insert new)
       - `void delete(Long id)` -- logic delete via MyBatis-Plus removeById (which does UPDATE deleted=1 due to @TableLogic)
       - `void batchDelete(List&lt;Long&gt; ids)` -- logic delete batch
       - `void setStatus(Long id, Integer status)` -- enable/disable per D-19
       - `void resetPassword(Long id)` -- reset to default password 'password123' (BCrypt encoded), set password_change_required=1 per D-14
       - `importCsv(MultipartFile file)` -- parse CSV via EasyExcel (sync reading, no write), match by username (工号), overwrite if exists per D-09, insert if not, return ImportResult with success/update/failure counts
       - Password validation helper: `isValidPassword(String pwd)` -- min 8 chars, contains letter AND number per D-12

       **RoleService / RoleServiceImpl:**
       - `PageResult&lt;RoleVO&gt; page(PageQuery dto)` -- paginated role list with user count subquery
       - `void create(RoleCreateDTO dto)` -- check roleCode unique
       - `void update(RoleUpdateDTO dto)` -- update role info
       - `void delete(Long id)` -- check if users have this role, prevent deletion if assigned (throw BusinessException)
       - `void assignMenuPermissions(Long roleId, List&lt;Long&gt; menuIds)` -- delete existing role_menu entries, batch insert new ones
       - `List&lt;MenuTreeNode&gt; getMenuTree(Long roleId)` -- returns full menu tree with checked state for given role

       **DepartmentService / DepartmentServiceImpl:**
       - `PageResult&lt;DepartmentVO&gt; page(PageQuery dto)` -- paginated, with member count subquery
       - `void create(DepartmentCreateDTO dto)` -- check deptCode unique
       - `void update(DepartmentUpdateDTO dto)`
       - `void delete(Long id)` -- check if users in department, block if members exist (throw BusinessException with count)
       - `List&lt;DepartmentVO&gt; listAll()` -- for dropdown options

       **DictCategoryService / DictCategoryServiceImpl:**
       - `List&lt;DictCategoryDTO&gt; listAll()` -- ordered by sort_order
       - `void create(DictCategoryDTO dto)` -- check code unique
       - `void update(DictCategoryDTO dto)`
       - `void delete(Long id)` -- check if entries exist, warn if any

       **DictEntryService / DictEntryServiceImpl:**
       - `PageResult&lt;DictEntryDTO&gt; page(Long categoryId, String keyword, PageQuery dto)` -- paginated, filter by category + keyword
       - `void create(DictEntryDTO dto)` -- check key unique within category
       - `void update(DictEntryDTO dto)`
       - `void delete(Long id)`

       **MenuService / MenuServiceImpl:**
       - `List&lt;MenuTreeNode&gt; buildTree()` -- load all sys_menu, build tree structure with parent-child relationships
       - `List&lt;MenuTreeNode&gt; getRoleCheckedTree(Long roleId)` -- full tree with checked flags for role

    3. Create REST controllers (all under `/api/system/`):
       - All controllers use @RestController, @RequestMapping("/api/system/{entity}")
       - All responses wrap in `Result&lt;T&gt;` (success/data/message format from common module)
       - Standard CRUD: @GetMapping("/page"), @GetMapping("/{id}"), @PostMapping, @PutMapping("/{id}"), @DeleteMapping("/{id}")
       - UserController: 
         - POST `/api/system/user/page` -- paginated list
         - POST `/api/system/user` -- create
         - PUT `/api/system/user/{id}` -- update
         - DELETE `/api/system/user/{id}` -- soft delete
         - POST `/api/system/user/batch-delete` -- batch delete
         - PUT `/api/system/user/{id}/status` -- enable/disable
         - POST `/api/system/user/{id}/reset-password` -- password reset per D-14
         - POST `/api/system/user/import` -- CSV import (multipart)
         - GET `/api/system/user/export` -- CSV export
       - RoleController:
         - POST `/api/system/role/page`, GET `/api/system/role/{id}`, POST, PUT, DELETE
         - GET `/api/system/role/{id}/menu-tree` -- get menu tree with checked state
         - PUT `/api/system/role/{id}/menu-permissions` -- assign menu permissions
         - GET `/api/system/role/list-all` -- list all roles for dropdown
       - DepartmentController:
         - POST `/api/system/department/page`, GET, POST, PUT, DELETE
         - GET `/api/system/department/list-all` -- for dropdown
       - DictCategoryController:
         - GET `/api/system/dict-category/list` -- list all categories
         - POST/PUT/DELETE for category CRUD
       - DictEntryController:
         - POST `/api/system/dict-entry/page`, POST, PUT, DELETE
       - MenuController:
         - GET `/api/system/menu/tree` -- full menu tree

    All services must handle standard error cases: unique constraint violations (check before insert), not found (throw EntityNotFoundException), and operation-not-allowed conditions (delete with existing references).

    Use `achievement-common/src/main/java/com/institute/achievement/common/exception/`:
    - `BaseException.java` (existing, abstract)
    - `BusinessException.java` (extends BaseException, for business logic errors)
    - `EntityNotFoundException.java` (extends BaseException, for not found)
    - `BadRequestException.java` (extends BaseException, for validation)

    Write all files via the Write tool. Ensure `./mvnw compile` succeeds.
  </action>
  <verify>
    <automated>./mvnw compile -pl achievement-module/achievement-system -am 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    1. `./mvnw compile -pl achievement-module/achievement-system -am` succeeds
    2. UserController has endpoints: /api/system/user/page, /api/system/user/import (POST, multipart), /api/system/user/{id}/reset-password
    3. UserServiceImpl.create encodes password with BCryptPasswordEncoder and validates min 8 + letter+number
    4. UserServiceImpl.delete uses logic delete (not physical delete)
    5. UserServiceImpl has CSV import matching by username (工号) with overwrite strategy per D-09
    6. RoleServiceImpl.delete checks if users have the role before allowing deletion
    7. DepartmentServiceImpl.delete checks if users are assigned before allowing deletion
    8. RoleController has GET /api/system/role/{id}/menu-tree returning MenuTreeNode with checked flag
    9. DictEntryServiceImpl checks unique key within category before insert
    10. All controllers return Result&lt;T&gt; wrapper
  </acceptance_criteria>
  <done>All backend CRUD services and REST APIs for user/role/department/dict complete. Controllers handle pagination, validation, error cases, CSV import with overwrite strategy per D-09. Logic delete implemented per D-20. Service layer compilation passes.</done>
</task>

<task type="auto">
  <name>Task 3: Create Frontend Pages (P-03 User Management, P-04 Role Management, P-05 Department Management, P-06 Data Dictionary)</name>
  <files>
    achievement-web/src/api/system/user.ts,
    achievement-web/src/api/system/role.ts,
    achievement-web/src/api/system/department.ts,
    achievement-web/src/api/system/dict.ts,
    achievement-web/src/views/system/user/index.vue,
    achievement-web/src/views/system/role/index.vue,
    achievement-web/src/views/system/department/index.vue,
    achievement-web/src/views/system/dict/index.vue,
    achievement-web/src/router/index.ts
  </files>
  <read_first>
    UI-SPEC.md: P-03 (User Management full contract), P-04 (Role Management with ElTree), P-05 (Department Management), P-06 (Data Dictionary left-tree-right-table)
    CONTEXT.md: D-06 (table+drawer), D-10 (left tree right table), D-11 (role multi-select), D-18 (tree permission), D-19 (enable/disable), D-20 (soft delete delete confirm copy)
    UI-SPEC.md Copywriting Contract section: empty states, error states, destructive actions confirmation copy
    UI-SPEC.md IP-01 (ElTag status colors), IP-02 (confirmation dialogs), IP-03 (drawer editing), IP-04 (batch operations), IP-06 (empty/loading/error states), IP-07 (pagination)
  </read_first>
  <action>
    Create API service modules and Vue components for all 4 management pages.

    1. Create API service modules under `src/api/system/`:

       **user.ts:**
       - `page(params)` -> POST /api/system/user/page
       - `create(data)` -> POST /api/system/user
       - `update(id, data)` -> PUT /api/system/user/${id}
       - `remove(id)` -> DELETE /api/system/user/${id}
       - `batchDelete(ids)` -> POST /api/system/user/batch-delete
       - `setStatus(id, status)` -> PUT /api/system/user/${id}/status
       - `resetPassword(id)` -> POST /api/system/user/${id}/reset-password
       - `importCsv(file)` -> POST /api/system/user/import (multipart/form-data)

       **role.ts:**
       - `page(params)` -> POST /api/system/role/page
       - `create(data)` -> POST
       - `update(id, data)` -> PUT
       - `remove(id)` -> DELETE
       - `getMenuTree(id)` -> GET /api/system/role/${id}/menu-tree
       - `assignMenuPermissions(id, menuIds)` -> PUT /api/system/role/${id}/menu-permissions
       - `listAll()` -> GET /api/system/role/list-all

       **department.ts:**
       - `page(params)`, `create`, `update`, `remove`
       - `listAll()` -> GET /api/system/department/list-all

       **dict.ts:**
       - Category: list(), create(data), update(id,data), remove(id)
       - Entry: page(categoryId, keyword, params), create(data), update(id,data), remove(id)

    2. Create `P-03 User Management` at `src/views/system/user/index.vue`:

       Full implementation matching UI-SPEC P-03 exact specification:
       - Search form: inline ElForm with keyword (ElInput), department (ElSelect, options from listAll), role (ElSelect), status (ElSelect: 全部/正常/停用), "搜索" + "重置" buttons (IP-05)
       - Toolbar: "新增用户" (primary+Plus), "导入" (Upload), "导出" (Download), "批量删除" (danger+Delete, disabled when none selected)
       - ElTable with @selection-change: checkbox, 序号, 用户名, 姓名, 部门, 角色 (ElTag group for multi-role), 手机号(masked: 138****1234), 邮箱, 状态 (ElTag: 正常=success, 停用=danger), 最后登录, 操作 (编辑/重置密码/启用or停用/删除 buttons)
       - ElPagination (IP-07): default 20, options 10/20/50/100, layout with total/sizes/prev/pager/next/jumper
       - ElDrawer editing (IP-03): 500px width, form with username, realName, department (ElSelect), role (ElSelect multiple, D-11), phone, email, status (ElSwitch)
         - Create mode: show password field (min 8, validate letter+number)
         - Edit mode: no password field, add inline "重置密码" button
       - ElDialog import: ElUpload drag-and-drop .csv, template download link, preview table first 5 rows, progress, result summary
       - Empty state (IP-06): "暂无用户数据" with CTA
       - Error state: ElAlert with retry
       - v-loading on table during fetch
       - ElMessageBox.confirm for destructive actions (IP-02): specific copy from UI-SPEC Copywriting Contract

    3. Create `P-04 Role Management` at `src/views/system/role/index.vue`:

       Full implementation matching UI-SPEC P-04:
       - Toolbar: "新增角色" button
       - ElTable: 序号, 角色名称, 角色标识 (monospace), 描述, 用户数 (clickable count), 创建时间, 操作 (编辑权限/编辑/删除)
       - ElDrawer for permission assignment (600px width): title "编辑权限 - {role_name}", ElTree with show-checkbox, default-expand-all, node-key="id", children auto cascade
         - Tree built from menu API response (MenuTreeNode recursive)
         - Search input to filter tree nodes (ElInput with filter-node-method)
       - ElDrawer for role editing (400px): roleName, roleCode, description, status
       - Delete confirmation: check if users assigned
       - Empty state: "暂无角色数据"
       - v-loading on tree

    4. Create `P-05 Department Management` at `src/views/system/department/index.vue`:

       Full implementation matching UI-SPEC P-05:
       - Toolbar: "新增部门" button
       - ElTable: 序号, 部门名称, 部门编码, 负责人, 联系电话, 成员数, 状态 (ElTag), 操作 (编辑/删除)
       - ElDrawer editing (400px width): deptName (required), deptCode (required), leader, phone, status (ElSwitch)
       - No parent department field -- flat structure per D-08
       - Delete: check if members exist, block with warning message per UI-SPEC
       - Empty state: "暂无部门数据"

    5. Create `P-06 Data Dictionary` at `src/views/system/dict/index.vue`:

       Full implementation matching UI-SPEC P-06:
       - Split panel layout: left 260px (ElTree for categories) + right rest (ElTable for entries)
       - Left panel: "新增分类" button + ElTree with category nodes
         - Root node: "全部字典"
         - Hover icons or context menu: 编辑分类 / 删除分类
       - Right panel: search ElInput + "新增条目" button + ElTable (字典键, 字典值, 排序, 状态 ElTag, 操作)
       - ElDrawer for entry editing (500px): category disabled select, dictKey (required, unique within category), dictValue (required), sortOrder (ElInputNumber), status
       - Small ElDialog for category editing (name, code, description, sort)
       - Selecting tree node filters right table; "全部字典" shows all
       - Category deletion check if entries exist
       - Empty state: "暂无字典条目" per category

    6. Update `src/router/index.ts` to:
       - Wire each view page to its route
       - Add `meta: { title: '用户管理', permission: 'system:user:list' }` etc. for RBAC route filtering (permission check to be enforced in Plan 00-03)
       - Keep redirect `/` -> `/system/user` for Phase 0 (since there's no dashboard yet with real data)

    All Vue components must use `<script setup lang="ts">` + `<template>` structure.
    Use Element Plus components per UI-SPEC inventory (ElTable, ElForm, ElDrawer, ElDialog, ElTag, ElTree, ElPagination, ElUpload, ElSelect, ElInput, ElButton, ElSwitch, ElMessage, ElMessageBox, ElAlert, ElEmpty, ElProgress).
    All text copy must match UI-SPEC Copywriting Contract exact strings.

    Write all files via the Write tool.
  </action>
  <verify>
    <automated>cd achievement-web && npx vue-tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    1. `npx vue-tsc --noEmit` passes without errors
    2. P-03 user/index.vue has: search ElForm with 4 fields (keyword + dept + role + status), toolbar buttons (新增/导入/导出/批量删除), ElTable with 11 columns per UI-SPEC, ElDrawer editing, ElDialog CSV import
    3. P-03 import dialog has ElUpload drag-and-drop .csv, template download link, preview table
    4. P-04 role/index.vue has: ElTable with 7 columns, ElDrawer (600px) with ElTree show-checkbox for permission assignment, filter input for tree search
    5. P-05 department/index.vue has: ElTable with 7 columns, ElDrawer (400px), no parent dept field per D-08
    6. P-06 dict/index.vue has: split panel left 260px ElTree + right ElTable, "全部字典" root node, entry drawer (500px)
    7. All API service files exist under src/api/system/ with correct endpoint paths
    8. Router has each view wired with permission meta field
    9. ElTag status colors match UI-SPEC IP-01 (正常=success, 停用=danger)
    10. Confirmation dialog copy matches UI-SPEC Copywriting Contract exact strings
  </acceptance_criteria>
  <done>All 4 system management pages fully implemented with Element Plus components matching UI-SPEC contracts. Backend API integration complete. TypeScript compilation passes. CSV import flow, multi-role assignment, tree-based permission assignment, and flat department CRUD all functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request -> REST controllers | Unvalidated input from frontend arrives at API endpoints |
| CSV file upload -> import service | External file content enters the system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-02-01 | Spoofing | UserController.create/update | mitigate | jakarta.validation @NotBlank/@Email/@Size on all DTOs |
| T-00-02-02 | Tampering | CSV import | mitigate | Validate each row, reject malformed records, max 1000 rows per upload, EasyExcel synchronous read (no streaming side-effect bypass) |
| T-00-02-03 | Information Disclosure | UserVO password field | mitigate | UserVO explicitly excludes password field (only entity has it) |
| T-00-02-04 | Elevation of Privilege | API endpoints without auth | accept | Auth filter added in Plan 00-03; endpoints are unprotected in dev during Plan 00-02 testing |
| T-00-02-05 | Repudiation | CRUD operations without logging | accept | Audit logging added in Plan 00-04 via AOP interceptor |
</threat_model>

<verification>
1. `./mvnw compile -pl achievement-module/achievement-system -am` succeeds
2. `cd achievement-web && npx vue-tsc --noEmit` succeeds
3. All 4 pages render with correct data from backend API (requires docker compose + application startup)
4. V1 migration creates all tables with seed data (7 roles, admin user, menu tree, dict categories)
5. Flyway migration applied on application boot
</verification>

<success_criteria>
- All backend CRUD APIs for user, role, department, dict category, and dict entry are working
- P-03 User Management page: search, paginate, create/edit via drawer, CSV import, enable/disable, reset password, soft delete, batch delete
- P-04 Role Management page: list roles, create/edit role, assign menu permissions via tree drawer
- P-05 Department Management page: list/create/edit/delete flat departments, block delete if members exist
- P-06 Data Dictionary page: left category tree, right entries table, CRUD for both categories and entries
- All destructive actions show confirmation dialogs with correct copy
- Empty/loading/error states handled on all pages
</success_criteria>

<output>
Create `.planning/phases/00-foundation-infrastructure/00-02-SUMMARY.md` when done
</output>
