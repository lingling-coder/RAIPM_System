---
phase: 00-foundation-infrastructure
plan: 04
type: execute
wave: 4
depends_on:
  - "00-03"
files_modified:
  # Backend audit log
  - achievement-framework/src/main/java/com/institute/achievement/framework/audit/AuditLogEntity.java
  - achievement-framework/src/main/java/com/institute/achievement/framework/audit/AuditLogMapper.java
  - achievement-framework/src/main/java/com/institute/achievement/framework/audit/AuditLogService.java
  - achievement-framework/src/main/java/com/institute/achievement/framework/audit/impl/AuditLogServiceImpl.java
  - achievement-framework/src/main/java/com/institute/achievement/framework/audit/AuditLogAspect.java
  - achievement-framework/src/main/java/com/institute/achievement/framework/audit/HashChainUtil.java
  - achievement-framework/src/main/java/com/institute/achievement/framework/audit/AuditLogController.java
  - achievement-module/achievement-system/src/main/resources/db/migration/V2__add_audit_log_file_ tables.sql
  # Backend file proxy
  - achievement-framework/src/main/java/com/institute/achievement/framework/file/FileStorageService.java
  - achievement-framework/src/main/java/com/institute/achievement/framework/file/impl/LocalFileStorageServiceImpl.java
  - achievement-framework/src/main/java/com/institute/achievement/framework/file/FileProxyController.java
  - achievement-framework/src/main/java/com/institute/achievement/framework/config/FileStorageConfig.java
  # Backend backup
  - achievement-framework/src/main/java/com/institute/achievement/framework/backup/BackupSchedulerService.java
  - achievement-framework/src/main/java/com/institute/achievement/framework/backup/BackupConfig.java
  # Config updates
  - achievement-framework/src/main/resources/application.yml
  - achievement-framework/src/main/resources/application-dev.yml
  # Frontend P-07
  - achievement-web/src/views/system/audit-log/index.vue
  - achievement-web/src/api/system/audit-log.ts
  - achievement-web/src/router/index.ts
autonomous: true
requirements:
  - SYS-07
  - OPS-01
  - OPS-02

must_haves:
  truths:
    - "Every write operation (login/logout, create, update, delete) is recorded in the audit_log table"
    - "Audit log records contain operator, timestamp, IP, operation type, target object, before/after content"
    - "Audit log table is partitioned by month (RANGE on created_at)"
    - "Each audit log entry stores the SHA-256 hash of the previous entry, forming a hash chain"
    - "Audit log entries cannot be deleted or modified (INSERT-only policy enforced at DB and app level)"
    - "Uploaded files are served through proxy URL with UUID filenames, direct storage paths are never exposed"
    - "Files are stored in uploads/YYYY/MM/type/ directory structure"
    - "Daily automated backup runs at midnight, backing up MySQL database + file storage directory"
    - "Backup files are retained for 30 days; older backups are automatically cleaned up"
    - "Audit log page (P-07) displays logs in a read-only table with operator/type/object/date filters and detail drawer with hash chain display"
  artifacts:
    - path: "achievement-module/achievement-system/src/main/resources/db/migration/V2__add_audit_log_file_tables.sql"
      provides: "Flyway migration creating audit_log (RANGE partitioned) and file_record tables"
      min_lines: 60
    - path: "achievement-framework/src/main/java/com/institute/achievement/framework/audit/AuditLogAspect.java"
      provides: "AOP @Around interceptor on all controller write methods to capture audit data"
      min_lines: 80
    - path: "achievement-framework/src/main/java/com/institute/achievement/framework/audit/HashChainUtil.java"
      provides: "SHA-256 hash chain utility: calculate hash, verify chain integrity"
      min_lines: 40
    - path: "achievement-framework/src/main/java/com/institute/achievement/framework/file/impl/LocalFileStorageServiceImpl.java"
      provides: "File storage: save to uploads/YYYY/MM/type/ with UUID filename, serve via proxy URL"
      min_lines: 80
    - path: "achievement-framework/src/main/java/com/institute/achievement/framework/backup/BackupSchedulerService.java"
      provides: "@Scheduled daily backup: mysqldump + tar + 30-day retention cleanup"
      min_lines: 60
    - path: "achievement-web/src/views/system/audit-log/index.vue"
      provides: "P-07 Audit Log page: read-only table with search/filter, detail drawer with hash chain display"
      min_lines: 250
  key_links:
    - from: "AuditLogAspect.java"
      to: "AuditLogService.java"
      via: "@Around advice calls auditLogService.record() after controller method execution"
      pattern: "auditLogService\\.record"
    - from: "AuditLogServiceImpl.java"
      to: "HashChainUtil.java"
      via: "record() computes previous log's SHA-256 hash and stores it as previousHash in new entry"
      pattern: "HashChainUtil\\.sha256"
    - from: "LocalFileStorageServiceImpl.java"
      to: "FileProxyController.java"
      via: "Controller serves files using the persisted UUID-to-path mapping from file_record table"
      pattern: "/api/files/"
    - from: "AuditLogServiceImpl.java"
      to: "AuditLogMapper.java"
      via: "INSERT-only operations (no update/delete methods on mapper)"
      pattern: "baseMapper\\.insert"
---

<objective>
Implement the append-only audit log system with hash-chain integrity (SYS-07), file proxy service with UUID naming and layered directories (OPS-02 / D-29/D-30/D-31), and daily automated backup scheduler (OPS-01 / D-32/D-33). Deliver P-07 Audit Log management page.

Purpose: Satisfy non-negotiable compliance requirements: audit trail integrity, file access security, and data backup/retention. The audit log ensures every write operation is permanently recorded with tamper detection. File proxy prevents direct path access. Daily backups meet RPO <= 30 minutes for data loss exposure.

Output: INSERT-only audit log with hash chain, monthly partitioned table, file proxy controller with UUID routing, daily mysqldump + file backup with 30-day cleanup, and functional P-07 page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation-infrastructure/00-CONTEXT.md (D-25 through D-33: audit, file, backup decisions)
@.planning/phases/00-foundation-infrastructure/00-UI-SPEC.md (P-07 Audit Log page contract, IP-01 ElTag status colors, IP-06 empty/loading/error states)
@.planning/REQUIREMENTS.md (SYS-07, OPS-01, OPS-02)
@CLAUDE.md (Spring @Scheduled + Redis distributed lock for backup, local file storage Phase 1)

<interfaces>
From Plan 00-03:
- SecurityContextHolder: get current username/IP for audit logging
- JwtAuthenticationFilter: ensures only authenticated users reach controller
- achievement-framework/security package for SecurityUtils helper (getCurrentUsername(), getCurrentUserId(), getClientIp())

From Plan 00-02:
- SysUser entity for audit operator name resolution
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Audit Log Flyway Migration + Entity + Hash Chain + AOP Interceptor + Controller</name>
  <files>
    achievement-module/achievement-system/src/main/resources/db/migration/V2__add_audit_log_file__tables.sql,
    achievement-framework/src/main/java/com/institute/achievement/framework/audit/AuditLogEntity.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/audit/AuditLogMapper.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/audit/AuditLogService.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/audit/impl/AuditLogServiceImpl.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/audit/AuditLogAspect.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/audit/HashChainUtil.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/audit/AuditLogVO.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/audit/AuditLogController.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/config/MyBatisPlusConfig.java
  </files>
  <read_first>
    CONTEXT.md: D-25 (all write operations: login/logout/CUD), D-26 (MySQL independent table + monthly partition), D-27 (SHA-256 hash chain, periodic integrity check), D-28 (audit log management page with filters)
    REQUIREMENTS.md: SYS-07 (operator/time/IP/before-after content, log not deletable, monthly partition, 5-year retention)
    UI-SPEC.md: P-07 full contract (search form with operator/type/object/date, table columns, detail drawer with hash display)
  </read_first>
  <action>
    Create the audit log system: DB migration, entity, hash chain utility, AOP interceptor, service, controller.

    1. Create Flyway migration `V2__add_audit_log_file__tables.sql`:

       **audit_log** table (D-26, D-27):
       ```sql
       CREATE TABLE audit_log (
         id BIGINT NOT NULL AUTO_INCREMENT,
         operator_id BIGINT,
         operator_name VARCHAR(50) NOT NULL COMMENT '操作人用户名',
         operation_type VARCHAR(50) NOT NULL COMMENT 'LOGIN/LOGOUT/CREATE/UPDATE/DELETE',
         operation_name VARCHAR(100) NOT NULL COMMENT '操作描述，如"登录系统"/"新增用户-张三"',
         target_type VARCHAR(100) COMMENT '操作对象类型，如"User"/"Role"',
         target_id VARCHAR(100) COMMENT '操作对象ID',
         target_content LONGTEXT COMMENT '操作后内容(JSON)',
         original_content LONGTEXT COMMENT '操作前内容(JSON)，CREATE/null',
         ip_address VARCHAR(50),
         user_agent VARCHAR(500),
         status TINYINT DEFAULT 1 COMMENT '1=成功, 0=失败',
         previous_hash VARCHAR(64) NOT NULL DEFAULT '' COMMENT '上一条日志的SHA-256哈希',
         current_hash VARCHAR(64) NOT NULL COMMENT '本条日志的SHA-256哈希',
         created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
         PRIMARY KEY (id, created_at)
       ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
         PARTITION BY RANGE (YEAR(created_at) * 100 + MONTH(created_at)) (
           PARTITION p202401 VALUES LESS THAN (202402),
           PARTITION p202402 VALUES LESS THAN (202403),
           -- repeat for 202403 through 202412, then:
           PARTITION p202501 VALUES LESS THAN (202502),
           PARTITION p202502 VALUES LESS THAN (202503),
           PARTITION p202503 VALUES LESS THAN (202504),
           PARTITION p202504 VALUES LESS THAN (202505),
           PARTITION p202505 VALUES LESS THAN (202506),
           PARTITION p202506 VALUES LESS THAN (202507),
           PARTITION p202507 VALUES LESS THAN (202508),
           PARTITION p202508 VALUES LESS THAN (202509),
           PARTITION p202509 VALUES LESS THAN (202510),
           PARTITION p202510 VALUES LESS THAN (202511),
           PARTITION p202511 VALUES LESS THAN (202512),
           PARTITION p202512 VALUES LESS THAN (202601),
           PARTITION p202601 VALUES LESS THAN (202602),
           PARTITION p202602 VALUES LESS THAN (202603),
           PARTITION p202603 VALUES LESS THAN (202604),
           PARTITION p202604 VALUES LESS THAN (202605),
           PARTITION p202605 VALUES LESS THAN (202606),
           PARTITION p202606 VALUES LESS THAN (202607),
           PARTITION p202607 VALUES LESS THAN (202608),
           PARTITION p202608 VALUES LESS THAN (202609),
           PARTITION p202609 VALUES LESS THAN (202610),
           PARTITION p202610 VALUES LESS THAN (202611),
           PARTITION p202611 VALUES LESS THAN (202612),
           PARTITION p202612 VALUES LESS THAN (202701),
           PARTITION p_future VALUES LESS THAN MAXVALUE
         );
       ```
       INDEXES:
       - `idx_audit_log_created_at` on (created_at)
       - `idx_audit_log_operator` on (operator_name)
       - `idx_audit_log_type` on (operation_type)
       - `idx_audit_log_target` on (target_type, target_id)

       Note: ALTER TABLE audit_log PARTITION BY RANGE is used if CREATE TABLE with partitions is not supported in Flyway. Use the create-with-partitions syntax above which works in MySQL 8.4.

       **file_record** table (for file proxy):
       ```sql
       CREATE TABLE file_record (
         id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
         original_name VARCHAR(500) NOT NULL COMMENT '原始文件名',
         stored_name VARCHAR(100) NOT NULL UNIQUE COMMENT 'UUID存储文件名',
         storage_path VARCHAR(500) NOT NULL COMMENT '相对存储路径',
         file_size BIGINT NOT NULL COMMENT '文件大小(字节)',
         mime_type VARCHAR(100) COMMENT 'MIME类型',
         file_type VARCHAR(50) NOT NULL COMMENT '类型标识，如PAPER/PATENT/SOFTWARE/AVATAR',
         upload_ip VARCHAR(50),
         created_by BIGINT,
         created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
         INDEX idx_file_record_type (file_type),
         INDEX idx_file_record_created_at (created_at)
       ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
       ```

    2. Create `HashChainUtil.java`:
       - `static String sha256(String input)`: SHA-256 hex digest of input string
       - `static String computeHash(Long id, Long previousId, String previousHash, String contentJson, LocalDateTime createdAt)`:
         - Input = id + previousId + previousHash + contentJson + createdAt.toString()
         - Return SHA-256 hex of concatenated input
       - `static boolean verifyChain(List&lt;AuditLogEntity&gt; logs)`:
         - Iterate through ordered list, verify each entry's current_hash matches recomputed value
         - Verify each entry's previous_hash matches previous entry's current_hash
       - `static String computePreviousHash(List&lt;AuditLogEntity&gt; logs)`:
         - Utility to get the last log's current_hash for a new entry

    3. Create `AuditLogEntity.java`:
       - `@TableName("audit_log")`, @Data
       - Fields: id (Long), operatorId (Long), operatorName, operationType, operationName, targetType, targetId, targetContent (String, JSON), originalContent (String, JSON), ipAddress, userAgent, status (Integer), previousHash, currentHash, createdAt (LocalDateTime)
       - @TableId on id
       - Note: MyBatis-Plus does not support INSERT-only by itself -- enforce via service layer (never expose update/delete methods on this mapper)

    4. Create `AuditLogMapper.java`:
       - Extends BaseMapper&lt;AuditLogEntity&gt;
       - Custom methods:
         - `Page&lt;AuditLogEntity&gt; selectPageWithFilters(Page&lt;AuditLogEntity&gt; page, @Param("operatorName") String operatorName, @Param("operationType") String operationType, @Param("targetType") String targetType, @Param("startTime") LocalDateTime startTime, @Param("endTime") LocalDateTime endTime)`: annotated @Select with dynamic WHERE conditions
         - `AuditLogEntity selectLastLog()`: `SELECT * FROM audit_log ORDER BY id DESC LIMIT 1`
         - `List&lt;AuditLogEntity&gt; selectChainSegment(@Param("fromId") Long fromId, @Param("toId") Long toId)`: `SELECT * FROM audit_log WHERE id BETWEEN #{fromId} AND #{toId} ORDER BY id ASC`
       - CRITICAL: Do NOT expose updateById or deleteById. The only write method available is `insert`.

    5. Create `AuditLogService` interface:
       - `void record(AuditLogDTO dto)`: record a new audit log entry with hash chain
       - `PageResult&lt;AuditLogVO&gt; page(AuditLogPageDTO dto)`: paginated query with filters
       - `AuditLogVO getDetail(Long id)`: get single log with hash info
       - `ChainVerificationResult verifyChain(Long fromId, Long toId)`: verify hash chain integrity for a range of entries
       - `Map&lt;String, Long&gt; getOperationTypeStats(LocalDateTime start, LocalDateTime end)`: count by operation type (for dashboard)

    6. Create `AuditLogServiceImpl` implementing AuditLogService:
       - `record(AuditLogDTO dto)`:
         - Get previous log's current_hash via auditLogMapper.selectLastLog() (or empty string if first log)
         - Build content JSON strings from request/response data
         - Compute current_hash using HashChainUtil
         - Set all fields on AuditLogEntity
         - `baseMapper.insert(entity)` -- only write operation allowed
       - `page(AuditLogPageDTO dto)`:
         - Call auditLogMapper.selectPageWithFilters with pagination
         - Wrap to VO
       - `verifyChain(fromId, toId)`:
         - Load chain segment
         - Verify each link, return result with boolean isValid + List&lt;ChainBreakVO&gt;
       - `getDetail(Long id)`:
         - Get entity by id
         - Compute expected hash and compare with stored (verification)
         - Return AuditLogVO with integrityStatus field

    7. Create helper DTOs:
       - `AuditLogDTO`: operatorId, operatorName, operationType, operationName, targetType, targetId, originalContent (Object -- will be JSON-serialized), targetContent (Object), ipAddress, userAgent, status
       - `AuditLogPageDTO`: operatorName, operationType, targetType, startTime, endTime (extend PageQuery)
       - `AuditLogVO`: id, operatorName, operationType, operationName, targetType, targetId, ipAddress, userAgent, status, originalContent, targetContent, previousHash, currentHash, integrityVerified (Boolean), createdAt
       - `ChainVerificationResult`: valid (boolean), brokenLinks (List&lt;ChainBreakVO&gt;), totalChecked (int)
       - `ChainBreakVO`: logId, expectedHash, actualHash, position

    8. Create `AuditLogAspect.java` (the key AOP interceptor per D-25):
       - @Aspect, @Component
       - `@Around("@annotation(org.springframework.web.bind.annotation.PostMapping) || @annotation(org.springframework.web.bind.annotation.PutMapping) || @annotation(org.springframework.web.bind.annotation.DeleteMapping)")`
       - Also intercept `AuthController.login()` and `AuthController.logout()` for LOGIN/LOGOUT events
       - Execution:
         - Proceed with the original method call
         - After proceed (in @AfterReturning or @Around with try/catch):
           - Extract: current user from SecurityContextHolder, IP from request, request body (originalContent for updates), response body (targetContent for creates/updates), target type from controller class/method mapping
           - Build AuditLogDTO with operation type based on annotation (POST=CREATE, PUT=UPDATE, DELETE=DELETE)
           - Call auditLogService.record(dto)
         - On exception: log with status=0 (failed)
       - Exclude GET requests (read operations not logged per D-25)
       - Exclude `/api/auth/refresh`, `/api/auth/logout`? No -- logout should be logged
       - For login: intercept specifically the AuthController.login method
       - For logout: intercept AuthController.logout or the filter's logout handler

       Implementation notes:
       - Use `@Around("controllerWriteOperation()")` with pointcut expression
       - Get HttpServletRequest from RequestContextHolder
       - For request body: use `ProceedingJoinPoint.getArgs()` to find @RequestBody parameter
       - For response: capture after method returns
       - For target type: extract from controller class simple name (e.g., "UserController" -> "User")
       - For operation description: build from controller name + method name + key identifier from request

    9. Create `AuditLogController.java` at `/api/system/audit-log`:
       - `@PostMapping("/page")`: paginated query with filters
       - `@GetMapping("/{id}")`: get detail with hash chain verification
       - `@PostMapping("/verify-chain")`: verify chain integrity for a range (fromId, toId)
       - `@PreAuthorize("hasRole('ROLE_SYSTEM_ADMIN') or hasRole('ROLE_AUDITOR')")` or @RequirePermission("system:audit:list")
       - READ-ONLY controller: no POST/PUT/DELETE on audit-log data itself

    10. Update `MyBatisPlusConfig.java` to ensure the audit_log mapper is scanned (add @MapperScan base package if needed).

    Create a `SecurityUtils.java` helper in `achievement-framework/src/main/java/com/institute/achievement/framework/security/`:
       - `static String getCurrentUsername()`: extract from SecurityContextHolder
       - `static Long getCurrentUserId()`: extract from JwtUser principal
       - `static String getClientIp()`: extract from HttpServletRequest (X-Forwarded-For or getRemoteAddr)
       - `static List&lt;String&gt; getCurrentPermissions()`: extract from authentication

    Ensure `./mvnw compile -pl achievement-module/achievement-system -am` succeeds.
    Write all files via the Write tool.
  </action>
  <verify>
    <automated>./mvnw compile -pl achievement-module/achievement-system -am 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    1. V2 migration creates audit_log table with PARTITION BY RANGE (YEAR(created_at)*100+MONTH(created_at)) per D-26
    2. audit_log table has previous_hash (VARCHAR 64) and current_hash (VARCHAR 64) columns per D-27
    3. AuditLogMapper only exposes insert (no update/delete methods) -- ENFORCE by not declaring them; verify via grep
    4. AuditLogServiceImpl.record() computes previousHash from last log and stores it; computes currentHash via HashChainUtil
    5. HashChainUtil.sha256() produces valid SHA-256 hex digest
    6. HashChainUtil.verifyChain() detects hash mismatch between two entries
    7. AuditLogAspect has @Around intercepting POST/PUT/DELETE controller methods
    8. AuditLogAspect captures: operatorName (from SecurityContext), IP (from request), operation type (from annotation), target content (from args)
    9. AuditLogController has POST /api/system/audit-log/page, GET /api/system/audit-log/{id}, POST /api/system/audit-log/verify-chain
    10. AuditLogController is read-only (no new/update/delete endpoints for audit log itself)
    11. `./mvnw compile -pl achievement-module/achievement-system -am` succeeds
  </acceptance_criteria>
  <done>Audit log system complete: Flyway migration with RANGE monthly partitioning (D-26), hash chain with SHA-256 (D-27), AOP interceptor capturing all write operations (D-25), read-only controller with filtering (D-28). Hash chain integrity verification endpoint available.</done>
</task>

<task type="auto">
  <name>Task 2: Create File Proxy Service + Daily Backup Scheduler</name>
  <files>
    achievement-framework/src/main/java/com/institute/achievement/framework/file/FileStorageService.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/file/impl/LocalFileStorageServiceImpl.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/file/FileProxyController.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/file/FileRecordEntity.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/file/FileRecordMapper.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/config/FileStorageConfig.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/backup/BackupSchedulerService.java,
    achievement-framework/src/main/java/com/institute/achievement/framework/backup/BackupConfig.java,
    achievement-framework/src/main/resources/application.yml,
    achievement-framework/src/main/resources/application-dev.yml
  </files>
  <read_first>
    CONTEXT.md: D-29 (uploads/YYYY/MM/type/ directory), D-30 (proxy URL access, UUID filename, no direct path exposure), D-31 (50MB limit, PDF/Word/Excel/Image/Zip types), D-32 (daily full backup DB + files), D-33 (30-day retention)
    REQUIREMENTS.md: OPS-01 (daily auto backup, 30-day retention), OPS-02 (file proxy, UUID names, no direct path)
    Task 1 V2 migration: file_record table schema
    CLAUDE.md: Spring @Scheduled for task scheduling (Phase 1 approach, XXL-JOB deferred to Phase 2)
  </read_first>
  <action>
    Create file proxy service with UUID naming and proxy-based access, plus daily backup scheduler.

    1. Create `FileStorageConfig.java`:
       - @ConfigurationProperties(prefix = "file.storage")
       - Fields: `uploadDir` (String, default "./uploads"), `maxFileSize` (long, default 52428800 = 50MB per D-31), `allowedTypes` (List&lt;String&gt;, default ["pdf","doc","docx","xls","xlsx","png","jpg","jpeg","gif","zip","rar"])
       - @EnableConfigurationProperties

    2. Create `FileStorageService` interface:
       - `FileRecordVO store(MultipartFile file, String fileType)`: save file, return record with UUID name + proxy URL
       - `Resource loadAsResource(String uuid)`: load file by UUID for proxy serving
       - `void delete(String uuid)`: soft delete file record (or physical delete for non-critical files)
       - `FileRecordVO getRecord(String uuid)`: get file metadata

    3. Create `FileRecordEntity.java`:
       - @TableName("file_record")
       - Fields: id, originalName, storedName (UUID), storagePath (relative path like "2026/06/paper/"), fileSize, mimeType, fileType, uploadIp, createdBy (Long), createdAt

    4. Create `FileRecordMapper.java`:
       - Extends BaseMapper&lt;FileRecordEntity&gt;
       - Custom: `FileRecordEntity findByStoredName(@Param("storedName") String storedName)`
       - No delete method exposed (keep file records)

    5. Create `LocalFileStorageServiceImpl.java`:
       - Implements FileStorageService
       - `store(MultipartFile file, String fileType)`:
         - Validate file size <= maxFileSize (50MB per D-31) -- throw FileTooLargeException
         - Validate file extension against allowedTypes -- throw UnsupportedFileTypeException
         - Generate UUID filename via UUID.randomUUID() (keep original extension)
         - Determine storage path: `{uploadDir}/{YYYY}/{MM}/{fileType}/` (D-29)
         - Create directories if not exist
         - Save file to disk using transferTo()
         - Create file_record entry: insert into database
         - Return FileRecordVO with: id, originalName, storedName (UUID), fileSize, mimeType, proxyUrl = "/api/files/" + storedName
       - `loadAsResource(String uuid)`:
         - Query file_record by storedName
         - If not found: throw FileNotFoundException
         - Build absolute path: `{uploadDir}/{storagePath}/{storedName}`
         - Return UrlResource (from Spring's ResourceLoader)
       - `getRecord(String uuid)`:
         - Query and return VO
       - Note on security: The proxy URL pattern `/api/files/{uuid}` means the real storage path is NEVER exposed to the client (D-30)

    6. Create `FileProxyController.java` at `/api/files/`:
       - `@GetMapping("/{uuid}")`:
         - Extract uuid from path
         - Call fileStorageService.loadAsResource(uuid)
         - Set Content-Type based on mime_type from file_record
         - Set Content-Disposition: inline (for browser preview) or attachment
         - Set Cache-Control: private, max-age=3600
         - Return ResponseEntity with Resource
       - `@PostMapping("/upload")`:
         - Accept MultipartFile + fileType parameter
         - Call fileStorageService.store()
         - Return FileRecordVO with proxyUrl
       - `@GetMapping("/{uuid}/info")`:
         - Return file metadata (FileRecordVO) without content
       - Security: All endpoints require authentication (Spring Security default from Plan 00-03)
       - No direct path download -- only proxy access via UUID

    7. Create `BackupConfig.java`:
       - @ConfigurationProperties(prefix = "backup")
       - Fields:
         - `enabled` (Boolean, default true)
         - `backupDir` (String, default "./backups")
         - `database.host` (String, default "localhost")
         - `database.port` (int, default 3306)
         - `database.username` (String, default "root")
         - `database.password` (String, default "root123")
         - `database.name` (String, default "achievement_db")
         - `retentionDays` (int, default 30 per D-33)
         - `cron` (String, default "0 0 2 * * ?") -- 2 AM daily
         - `fileBackupPaths` (List&lt;String&gt;, default containing uploadDir path)
       - @EnableConfigurationProperties

    8. Create `BackupSchedulerService.java`:
       - @Service
       - `@Scheduled(cron = "${backup.cron}")` -- daily backup trigger
       - `backup()`:
         - Step 1: Create timestamped backup directory: `{backupDir}/achievement-{yyyyMMdd-HHmmss}/`
         - Step 2: Database backup via ProcessBuilder:
           - Execute: `mysqldump -h {host} -P {port} -u {username} -p{password} {dbName} --routines --triggers --single-transaction > {backupDir}/achievement-db-{timestamp}.sql`
           - Also dump the achievement_classified schema if configured
         - Step 3: File backup via tar:
           - Execute: `tar -czf {backupDir}/uploads-{timestamp}.tar.gz {uploadDir}`
         - Step 4: Retention cleanup:
           - List backup directories sorted by creation time
           - Remove directories older than retentionDays
           - Use Files.walk() to find and delete old backups
         - Step 5: Record backup result in system log or a backup_audit table
         - Handle errors: if backup fails, log error (do not crash scheduler)
       - For Windows compatibility:
         - Use `mysqldump` directly (assumes mysqldump is in PATH -- document as user setup dependency)
         - For file backup, use a simple zip/robocopy approach since Windows may not have tar
         - Alternative: use Java ZipOutputStream to create file backup on Windows
         - Detect OS: System.getProperty("os.name").startsWith("Windows") to switch between tar and zip

    9. Update `achievement-framework/src/main/resources/application.yml`:
       ```yaml
       file:
         storage:
           upload-dir: ./uploads
           max-file-size: 52428800  # 50MB
           allowed-types: pdf,doc,docx,xls,xlsx,png,jpg,jpeg,gif,zip,rar

       backup:
         enabled: true
         backup-dir: ./backups
         database:
           host: localhost
           port: 3306
           username: root
           password: root123
           name: achievement_db
         retention-days: 30
         cron: 0 0 2 * * ?  # 2 AM daily
         file-backup-paths: ./uploads
       ```

    10. Update `achievement-module/achievement-system/src/main/resources/application.yml` or the framework's application.yml to enable scheduling:
        - `spring.task.scheduling.pool.size: 2`
        - Add `@EnableScheduling` annotation on AchievementSystemApplication class (or on a separate @Configuration class)

    Ensure `./mvnw compile -pl achievement-module/achievement-system -am` succeeds.
    Write all files via the Write tool.
  </action>
  <verify>
    <automated>./mvnw compile -pl achievement-module/achievement-system -am 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    1. LocalFileStorageServiceImpl.store() validates file size <= 52428800 (50MB) per D-31
    2. LocalFileStorageServiceImpl.store() validates file extension against allowed types
    3. LocalFileStorageServiceImpl.store() generates UUID filename and saves to uploads/YYYY/MM/type/ directory per D-29
    4. FileProxyController returns file via /api/files/{uuid} proxy URL -- no direct path exposure per D-30
    5. FileProxyController has GET /api/files/{uuid} and POST /api/files/upload endpoints
    6. BackupSchedulerService has @Scheduled(cron) method for daily backup
    7. BackupSchedulerService.backup() performs mysqldump + file backup (tar on Linux, zip on Windows)
    8. BackupSchedulerService cleanup removes backups older than retentionDays (30 days per D-33)
    9. FileStorageConfig reads upload-dir, max-file-size, allowed-types from application.yml
    10. `./mvnw compile -pl achievement-module/achievement-system -am` succeeds
  </acceptance_criteria>
  <done>File proxy service complete: UUID naming, layered directory storage (YYYY/MM/type/), 50MB limit, proxy-based access (no direct path exposure per D-30). Daily backup scheduler with mysqldump + file archiving + 30-day retention cleanup per D-32/D-33. Windows-compatible zip fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Create P-07 Audit Log Management Page</name>
  <files>
    achievement-web/src/views/system/audit-log/index.vue,
    achievement-web/src/api/system/audit-log.ts,
    achievement-web/src/router/index.ts
  </files>
  <read_first>
    UI-SPEC.md: P-07 (Audit Log Page full contract -- filter form, table columns, detail drawer, hash chain display, read-only, export CSV)
    UI-SPEC.md Copywriting Contract: empty state text, error state text
    UI-SPEC.md IP-01 (ElTag status colors for operation types), IP-02 (confirmation -- not needed, read-only), IP-06 (empty/loading/error states), IP-07 (pagination)
  </read_first>
  <action>
    Create the P-07 Audit Log management page with full frontend implementation.

    1. Create `src/api/system/audit-log.ts`:
       - `page(params)` -> POST /api/system/audit-log/page
       - `getDetail(id)` -> GET /api/system/audit-log/${id}
       - `verifyChain(fromId, toId)` -> POST /api/system/audit-log/verify-chain

    2. Create `src/views/system/audit-log/index.vue`:

       Full implementation matching UI-SPEC P-07 exact specification:

       **Search/Filter Panel** (collapsible ElForm, 4-row inline):
       - 操作人 (ElInput, placeholder "搜索操作人")
       - 操作类型 (ElSelect, multiple selectable: 登录/登出/新增/编辑/删除)
       - 操作对象 (ElInput, placeholder "搜索操作对象")
       - 时间范围 (ElDatePicker type="datetimerange", default last 7 days)
       - "搜索" (ElButton primary) + "重置" (ElButton default) per IP-05
       - Default time range: last 7 days

       **Table Columns (ElTable, read-only, no edit/create/delete buttons):
       | Column | Width | Render |
       |--------|-------|--------|
       | 序号 | 60px | index |
       | 操作时间 | 160px | YYYY-MM-DD HH:mm:ss |
       | 操作人 | 120px | text (operator_name) |
       | 操作类型 | 100px | ElTag: 登录=info, 登出=info, 新增=success, 编辑=warning, 删除=danger |
       | 操作对象 | 200px | text (e.g., "用户-张三") |
       | 对象ID | 120px | monospace text |
       | IP地址 | 130px | text |
       | 操作结果 | 90px | ElTag: 成功=success, 失败=danger |
       | 操作 | 100px | ElButton text "查看详情" link |

       **ElDrawer (600px, read-only detail drawer per UI-SPEC):
       - Title: "操作详情"
       - ElDescriptions with border:
         - 操作时间, 操作人, 操作类型, 操作对象, 对象ID, IP地址, User-Agent
         - 变更前内容: ElInput type="textarea" readonly rows=6, shows originalContent JSON
         - 变更后内容: ElInput type="textarea" readonly rows=6, shows targetContent JSON
       - Hash chain section:
         - 当前哈希: Text in monospace font (monospace)
         - 上一条哈希: Text in monospace font
         - 链条状态: ElTag "链条完整" (success) if integrityVerified=true, "链条断裂" (danger) if false
       - No edit/save/cancel buttons (read-only drawer)

       **Toolbar:
       - "导出" (ElButton default, icon Download): exports filtered results as CSV
       - No "新增" or other create buttons (read-only page per D-25)

       **States per UI-SPEC:
       - Empty: ElEmpty "暂无审计日志" with secondary "调整筛选条件后重新查询"
       - Loading: ElTable v-loading
       - Error: ElAlert "审计日志加载失败" with "点击重试"
       - Detail loading: v-loading on drawer

       **Pagination: Standard ElPagination per IP-07 (default 20, options 10/20/50/100)

       **Hash chain verification:
       - Add a "完整性校验" button in toolbar
       - When clicked, calls verifyChain API
       - Results shown in ElResult component: "校验通过: X 条日志链条完整" (success) or "校验发现 X 处链条断裂" (danger)
       - Show details of broken links in a small ElTable

    3. Update `src/router/index.ts` to wire the audit log page:
       - Route: /system/audit-log -> lazy import @/views/system/audit-log/index.vue
       - meta: { title: '审计日志', permission: 'system:audit:list' }

    All Vue code uses `<script setup lang="ts">` + `<template>`.
    All text copy must match UI-SPEC Copywriting Contract exact strings.
    Write all files via the Write tool.
  </action>
  <verify>
    <automated>cd achievement-web && npx vue-tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    1. P-07 audit-log page has: search form with operator/type/object/date fields, read-only ElTable, detail drawer (600px)
    2. Operation type ElTags match: 登录=info, 新增=success, 编辑=warning, 删除=danger, 登出=info per UI-SPEC
    3. Detail drawer shows ElDescriptions with operators/time/details + hash chain display (currentHash/previousHash in monospace) + integrity ElTag
    4. "完整性校验" button calls verifyChain API and shows results in ElResult
    5. "导出" button for CSV export
    6. Empty state: "暂无审计日志，调整筛选条件后重新查询"
    7. Error state: ElAlert "审计日志加载失败" with retry
    8. Default time range is last 7 days
    9. Route /system/audit-log wired with permission meta
    10. `npx vue-tsc --noEmit` passes without errors
  </acceptance_criteria>
  <done>P-07 Audit Log management page fully implemented per UI-SPEC contract. Read-only table with multi-dimensional filtering, detail drawer with hash chain integrity display, chain verification tool. ElTag colors match operation types. TypeScript compilation passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File upload endpoint | External files enter the server filesystem |
| File proxy download | Internal files leave the server via UUID proxy |
| Audit log records | Write operations captured by AOP interceptor |
| Backup script execution | Server command execution for mysqldump/tar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-04-01 | Tampering | Audit log hash chain | mitigate | Hash chain: each entry stores SHA-256 of previous entry. verifyChain endpoint detects breaks. App-level: no update/delete methods on AuditLogMapper. DB-level: INSERT-only application logic (no cascade/update grants in prod schema) |
| T-00-04-02 | Elevation of Privilege | File path traversal via proxy | mitigate | FileProxyController only loads by UUID (not user-supplied path). UUID is a 36-char hex string with hyphens -- path traversal impossible. Query from DB, construct path internally |
| T-00-04-03 | Denial of Service | Large file upload | mitigate | LocalFileStorageServiceImpl checks file size <= 50MB at controller entry. Spring's multipart max file size also configured. Disk space monitoring TBD for Phase 2 |
| T-00-04-04 | Information Disclosure | Backup file exposure | mitigate | Backups stored in ./backups/ directory outside web root. No web-accessible path points to backup dir |
| T-00-04-05 | Elevation of Privilege | Command injection in backup | mitigate | BackupSchedulerService uses ProcessBuilder with fixed commands and config-driven parameters (host/port/user/pass from config, not user input). mysqldump path is hardcoded or from PATH -- no user-supplied shell arguments |
| T-00-04-06 | Tampering | Audit log record forgery | accept | Internal threat: sys admin with DB access can modify audit_log directly. Mitigation: hash chain makes undetected modification infeasible (verification detects breaks). Full mitigation requires append-only DB triggers or separate audit database -- deferred to Phase 2 |
</threat_model>

<verification>
1. `./mvnw compile -pl achievement-module/achievement-system -am` succeeds
2. `cd achievement-web && npx vue-tsc --noEmit` succeeds
3. Application startup applies V2 migration creating audit_log (with partitions) and file_record tables
4. Create/update/delete operations on users/roles/departments result in audit log entries
5. Audit log entries have valid hash chain (previous_hash links correctly)
6. File upload returns proxy URL with UUID; accessing that URL serves the file
7. Direct path access to uploads/ directory is not web-accessible
8. Backup scheduler would run (manual test: trigger backup method via actuator or test)
</verification>

<success_criteria>
- Audit log records every write operation with operator, time, IP, before/after content
- Hash chain integrity verifiable via API; tampering detected
- Audit table partitioned by month with p_future catch-all partition
- File upload generates UUID filename, stored in uploads/YYYY/MM/type/, served via /api/files/{uuid}
- File size limit 50MB enforced; type whitelist enforced
- Daily backup: database (mysqldump) + files (tar/zip), auto-clean backups older than 30 days
- P-07 Audit Log page renders with all filters, read-only table, detail drawer with hash display
</success_criteria>

<output>
Create `.planning/phases/00-foundation-infrastructure/00-04-SUMMARY.md` when done
</output>
