---
phase: 00-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - achievement-common/pom.xml
  - achievement-framework/pom.xml
  - achievement-module/pom.xml
  - achievement-module/achievement-system/pom.xml
  - achievement-web/pom.xml
  - docker-compose.yml
  - .mvn/wrapper/maven-wrapper.properties
  - mvnw
  - mvnw.cmd
  - .gitignore
  - achievement-common/src/main/java/com/institute/achievement/common/exception/BaseException.java
  - achievement-common/src/main/java/com/institute/achievement/common/constant/ApiConstants.java
  - achievement-common/src/main/resources/application.yml
  - achievement-framework/src/main/resources/application.yml
  - achievement-framework/src/main/java/com/institute/achievement/framework/config/MyBatisPlusConfig.java
  - achievement-framework/src/main/java/com/institute/achievement/framework/config/JacksonConfig.java
  - achievement-framework/src/main/java/com/institute/achievement/framework/config/CorsConfig.java
  - achievement-framework/src/main/java/com/institute/achievement/framework/config/RedisConfig.java
  - achievement-module/achievement-system/src/main/resources/application.yml
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/AchievementSystemApplication.java
  - achievement-web/package.json
  - achievement-web/vite.config.ts
  - achievement-web/src/layout/index.vue (Vue Pure Admin layout shell)
autonomous: true
requirements: []
user_setup:
  - service: Docker
    why: "Local development MySQL 8.4 + Redis 7.x"
    env_vars: []
    dashboard_config:
      - task: "Install Docker Desktop 4.x+ and enable WSL2 backend"
        location: "https://docs.docker.com/desktop/install/windows-install/"
  - service: Java JDK 21
    why: "Compile and run Spring Boot 4.1"
    env_vars: []
    dashboard_config:
      - task: "Download and install JDK 21 LTS (Eclipse Temurin or Oracle)"
        location: "https://adoptium.net/temurin/releases/?version=21"

must_haves:
  truths:
    - "Developer can clone repo, run `mvnw clean install`, and see BUILD SUCCESS"
    - "Developer can run `docker compose up -d` and MySQL 8.4 + Redis 7.x start on standard ports"
    - "Developer can open achievement-web and run `npm run dev` to see Vue Pure Admin dev server"
    - "Maven multi-module structure compiles: achievement-common, achievemement-framework, achievement-module/achievement-system"
    - "All 4+1 modules exist as directories with valid POMs"
  artifacts:
    - path: "pom.xml"
      provides: "Parent POM declaring all 4+1 modules, Spring Boot 4.1.x parent, dependency management"
      contains: "<modules>"
    - path: "docker-compose.yml"
      provides: "MySQL 8.4 + Redis 7.x containers with named volumes, port mapping"
      contains: "mysql:8.4"
    - path: ".mvn/wrapper/maven-wrapper.properties"
      provides: "Maven Wrapper version pinning"
      contains: "maven"
    - path: "achievement-web/package.json"
      provides: "Vue Pure Admin + Element Plus dependencies declared"
      contains: "\"vue\""
  key_links:
    - from: "pom.xml"
      to: "achievement-common/pom.xml"
      via: "Maven module declaration"
      pattern: "<module>achievement-common</module>"
---

<objective>
Initialize the 4+1 Maven multi-module project structure, Docker Compose for local MySQL 8.4 + Redis 7.x, Maven Wrapper, and Vue Pure Admin frontend scaffold.

Purpose: Establish the project skeleton that all subsequent plans build on. This is a one-time structural setup -- every plan from 00-02 onward depends on this foundation.

Output: A compilable monorepo with backend modules (achievement-common, achievement-framework, achievement-module/achievement-system) and a runnable Vue Pure Admin frontend (achievement-web).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 0 goal, success criteria)
@.planning/PROJECT.md (architecture constraints, user roles)
@.planning/phases/00-foundation-infrastructure/00-CONTEXT.md (D-01 through D-05: module structure, naming, Docker, Monorepo, Vue Pure Admin)
@CLAUDE.md (tech stack versions: Spring Boot 4.1.x, JDK 21, MyBatis-Plus 3.5.x, MySQL 8.4 LTS, Redis 7.x, Vue 3.4+, Element Plus 2.9+, Vite 5)

<interfaces>
This plan creates the project skeleton. No existing interfaces to extract -- greenfield project.
Key conventions established:
- Group ID: com.institute
- Artifact naming: achievement-{module}
- Top-level package: com.institute.achievement.{module}
- Spring Boot parent: org.springframework.boot:spring-boot-starter-parent:4.1.x
- Application entry: AchievementSystemApplication.java (in achievement-system module)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Parent POM with Multi-Module Declaration + Maven Wrapper</name>
  <files>
    pom.xml,
    achievement-common/pom.xml,
    achievement-framework/pom.xml,
    achievement-module/pom.xml,
    achievement-module/achievement-system/pom.xml,
    achievement-web/pom.xml,
    .mvn/wrapper/maven-wrapper.properties,
    mvnw,
    mvnw.cmd,
    .gitignore
  </files>
  <read_first>
    Source of truth: CONTEXT.md D-01 (4+1 module structure), D-02 (naming convention), D-03 (Maven Wrapper), D-04 (Monorepo)
    Tech stack versions: CLAUDE.md Technology Stack table (Spring Boot 4.1.x, JDK 21, MyBatis-Plus 3.5.7, MySQL 8.4 LTS, Redis 7.x, SpringDoc OpenAPI 3.0.x, MapStruct 1.6.x, EasyExcel 4.0.x, Resilience4j)
    POM structure reference: standard Spring Boot 4.1.x multi-module POM with Jakarta EE 11 dependency management
  </read_first>
  <action>
    Create the root directory at project root. Then:

    1. Create root `pom.xml` with:
       - Parent: `org.springframework.boot:spring-boot-starter-parent:4.1.0`
       - Group ID: `com.institute`, Artifact ID: `achievement-parent`
       - Packaging: `pom`
       - Modules declared in order: `achievement-common`, `achievement-framework`, `achievement-module/achievement-system`, `achievement-web`
       - Java version: 21, encoding: UTF-8
       - `<dependencyManagement>` section managing versions for: spring-cloud (2025.0.0 optional), mybatis-plus-boot-starter 3.5.7, springdoc-openapi-starter-webmvc-ui 3.0.2, mapstruct 1.6.3, easyexcel 4.0.3, resilience4j-spring-boot2 3.0.3 (as resilience4j-spring-boot3 when available), jjwt-api 0.12.5, redisson-spring-boot-starter 3.30.0
       - `<properties>`: `<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>`, `<java.version>21</java.version>`, `<maven.compiler.source>21</maven.compiler.source>`, `<maven.compiler.target>21</maven.compiler.target>`
       - `<build><plugins>`: spring-boot-maven-plugin (skip on achievement-common and achievement-framework), maven-compiler-plugin with annotationProcessorPaths for mapstruct-processor and lombok
       - Repositories: Maven Central only (no additional repos)

    2. Create `achievement-common/pom.xml`:
       - Parent: achievement-parent
       - Artifact ID: `achievement-common`
       - Dependencies: spring-boot-starter-web (provided, for exception types used by framework), lombok, mapstruct, mapstruct-processor (annotation processor), hutool-all 5.8.30 (utility library for Chinese enterprise projects -- StringUtils, DateUtil, IdUtil), jackson-databind (managed by Spring Boot)
       - Package structure scaffold: `src/main/java/com/institute/achievement/common/` with subdirs `exception/`, `constant/`, `enums/`, `util/`
       - Create `BaseException.java` (abstract, extends RuntimeException), `ApiConstants.java` (HTTP header constants, pagination defaults), `ResultCode.java` (enum: SUCCESS=200, CREATED=201, BAD_REQUEST=400, UNAUTHORIZED=401, FORBIDDEN=403, NOT_FOUND=404, CONFLICT=409, INTERNAL_ERROR=500, LOCKED=423), `PageQuery.java` (page/pageSize fields, defaults 1/20), `PageResult.java` (records/total/page/pageSize generic wrapper)

    3. Create `achievement-framework/pom.xml`:
       - Parent: achievement-parent
       - Artifact ID: `achievement-framework`
       - Dependencies: achievement-common, spring-boot-starter-web, spring-boot-starter-security, spring-boot-starter-data-redis, mybatis-plus-boot-starter 3.5.7, springdoc-openapi-starter-webmvc-ui 3.0.2, redisson-spring-boot-starter 3.30.0, jjwt-api 0.12.5 + jjwt-impl + jjwt-jackson, resilience4j-spring-boot3 3.0.3, mapstruct, mapstruct-processor (annotation processor), lombok, spring-boot-starter-aop, mysql-connector-j (8.4.x), flyway-core + flyway-mysql
       - Package structure: `src/main/java/com/institute/achievement/framework/config/` with empty config classes (they'll be populated by later plans -- create empty stubs only): MyBatisPlusConfig.java (@Configuration @MapperScan), JacksonConfig.java, CorsConfig.java, RedisConfig.java

    4. Create `achievement-module/pom.xml`:
       - Parent: achievement-parent
       - Artifact ID: `achievement-module`
       - Packaging: pom
       - Modules: `achievement-system`

    5. Create `achievement-module/achievement-system/pom.xml`:
       - Parent: achievement-parent
       - Artifact ID: `achievement-system`
       - Dependencies: achievement-framework, spring-boot-starter-web, mybatis-plus-spring-boot-starter 3.5.7, mapstruct, mapstruct-processor, lombok
       - Package structure: `src/main/java/com/institute/achievement/module/system/` with subdirs `entity/`, `mapper/`, `service/`, `controller/`, `dto/`
       - Create `AchievementSystemApplication.java` with @SpringBootApplication, @MapperScan("com.institute.achievement.module.system.mapper"), main method calling SpringApplication.run
       - Create `src/main/resources/application.yml` with minimal config (spring.application.name: achievement-system, server.port: 8080)
       - Create `src/main/resources/db/migration/` (empty dir for Flyway)

    6. Create `achievement-web/pom.xml`:
       - Parent: achievement-parent
       - Artifact ID: `achievement-web`
       - Packaging: pom (this module just wraps the Vue build via maven-antrun-plugin or frontend-maven-plugin -- skip for now, Vue build is managed via npm)

    7. Create `.mvn/wrapper/maven-wrapper.properties` with `distributionUrl=https://repo.maven.apache.org/maven2/org/apache/maven/apache-maven/3.9.9/apache-maven-3.9.9-bin.zip`

    8. Run `mvnw -N wrapper:wrapper -Dmaven=3.9.9` to generate mvnw/mvnw.cmd scripts. If mvnw script generation fails (no Maven in PATH), manually download Maven Wrapper JAR to `.mvn/wrapper/` and create the mvnw/mvnw.cmd from the standard Apache Maven Wrapper distribution, using the distributionUrl from step 7.

    9. Create `.gitignore` with: `target/`, `node_modules/`, `.idea/`, `*.iml`, `.vscode/`, `*.log`, `.DS_Store`, `docker-data/`, `.env`, `application-local.yml`

    Write all POM files using the Write tool. Create Java package directories (they become git-tracked when a .keep file is added inside `empty directories`) by including `BaseException.java`, `ApiConstants.java`, etc. so the directories are not empty.
  </action>
  <verify>
    <automated>cd /d/repo-root && ./mvnw clean install -DskipTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    1. `./mvnw clean install -DskipTests` exits with BUILD SUCCESS (exit code 0)
    2. ls `pom.xml`, `achievement-common/pom.xml`, `achievement-framework/pom.xml`, `achievement-module/pom.xml`, `achievement-module/achievement-system/pom.xml` all exist
    3. `achievement-common/src/main/java/com/institute/achievement/common/constant/ApiConstants.java` exists with `public static final int DEFAULT_PAGE_SIZE = 20`
    4. `achievement-module/achievement-system/src/main/java/com/institute/achievement/module/system/AchievementSystemApplication.java` has @SpringBootApplication
    5. `.gitignore` contains `target/` and `node_modules/`
  </acceptance_criteria>
  <done>Maven multi-module project compiles successfully. All 4+1 modules declared in parent POM. achievement-system has a @SpringBootApplication entry point. Common utilities package scaffolded.</done>
</task>

<task type="auto">
  <name>Task 2: Create Docker Compose + Application Configuration Files</name>
  <files>
    docker-compose.yml,
    achievement-framework/src/main/resources/application.yml,
    achievement-framework/src/main/resources/application-dev.yml,
    achievement-common/src/main/resources/application.yml,
    achievement-module/achievement-system/src/main/resources/application.yml
  </files>
  <read_first>
    CONTEXT.md D-03 (Docker Compose for MySQL 8.4 + Redis 7.x)
    CLAUDE.md tech stack table for MySQL and Redis versions
    Task 1 output: achievement-parent POM structure so packaging config layer alignment is clear
  </read_first>
  <action>
    1. Create `docker-compose.yml` at project root:
       - Services:
         - `mysql`:
           - image: `mysql:8.4`
           - container_name: `achievement-mysql`
           - environment: `MYSQL_ROOT_PASSWORD=root123`, `MYSQL_DATABASE=achievement_db`, `MYSQL_CHARACTER_SET_SERVER=utf8mb4`, `MYSQL_COLLATION_SERVER=utf8mb4_unicode_ci`
           - ports: `3306:3306`
           - volumes: `achievement-mysql-data:/var/lib/mysql` (named volume), `./db/init:/docker-entrypoint-initdb.d` (init scripts)
           - command: `--character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci --innodb_buffer_pool_size=512M --max_allowed_packet=64M`
           - healthcheck: `mysqladmin ping -h localhost -proot123`
         - `redis`:
           - image: `redis:7`
           - container_name: `achievement-redis`
           - ports: `6379:6379`
           - volumes: `achievement-redis-data:/data`
           - command: `redis-server --appendonly yes --requirepass redis123`
       - Volumes: `achievement-mysql-data:`, `achievement-redis-data:`
       - networks: none needed (default bridge)

    2. Create `db/init/01-init.sql` with SQL to create the achievement_db database and the `classified` schema (D-39):
       ```sql
       CREATE DATABASE IF NOT EXISTS achievement_db DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
       CREATE DATABASE IF NOT EXISTS achievement_classified DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
       ```

    3. Create `achievement-framework/src/main/resources/application.yml` (base config shared by all modules):
       - `spring.profiles.active: dev`
       - `spring.jackson.date-format: yyyy-MM-dd HH:mm:ss`
       - `spring.jackson.time-zone: Asia/Shanghai`
       - `spring.jackson.default-property-inclusion: non_null`
       - `server.servlet.encoding.charset: UTF-8`
       - `server.servlet.encoding.enabled: true`
       - `server.servlet.encoding.force: true`

    4. Create `achievement-framework/src/main/resources/application-dev.yml`:
       - `spring.datasource.url: jdbc:mysql://localhost:3306/achievement_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai`
       - `spring.datasource.username: root`
       - `spring.datasource.password: root123`
       - `spring.datasource.driver-class-name: com.mysql.cj.jdbc.Driver`
       - `spring.flyway.enabled: true`
       - `spring.flyway.locations: classpath:db/migration`
       - `spring.flyway.baseline-on-migrate: true`
       - `spring.data.redis.host: localhost`
       - `spring.data.redis.port: 6379`
       - `spring.data.redis.password: redis123`
       - `spring.data.redis.database: 0`
       - `spring.data.redis.timeout: 5000`
       - `mybatis-plus.configuration.log-impl: org.apache.ibatis.logging.stdout.StdOutImpl` (dev SQL logging)
       - `mybatis-plus.global-config.db-config.logic-delete-field: deleted`
       - `mybatis-plus.global-config.db-config.logic-delete-value: 1`
       - `mybatis-plus.global-config.db-config.logic-not-delete-value: 0`
       - `resilience4j.retry.configs.default.max-attempts: 3`
       - `resilience4j.retry.configs.default.wait-duration: 1s`
       - `resilience4j.retry.configs.default.enable-exponential-backoff: true`
       - `resilience4j.retry.configs.default.exponential-backoff-multiplier: 2`

    5. Create `achievement-module/achievement-system/src/main/resources/application.yml`:
       - `spring.application.name: achievement-system`
       - `server.port: 8080`
       - (framework application.yml is inherited via classpath; this overrides app-specific)

    All config files use YAML format. Do not use .properties files.
    Use the Write tool for all files.
  </action>
  <verify>
    <automated>docker compose config 2>&1 | grep -q "services" && echo "Docker Compose valid"</automated>
  </verify>
  <acceptance_criteria>
    1. `docker-compose.yml` has both `mysql:8.4` and `redis:7` services with port mappings and named volumes
    2. `docker compose config` runs without errors (validates YAML syntax and structure)
    3. `db/init/01-init.sql` contains `CREATE DATABASE achievement_classified` for D-39
    4. `application-dev.yml` has `spring.datasource.url=jdbc:mysql://localhost:3306/achievement_db` and `spring.data.redis.password=redis123`
    5. `application-dev.yml` has mybatis-plus logic-delete config (deleted=1, not-deleted=0)
    6. `application-dev.yml` has `resilience4j.retry.configs.default.exponential-backoff-multiplier: 2`
  </acceptance_criteria>
  <done>Docker Compose file defines MySQL 8.4 + Redis 7.x containers. Application configs set up for dev profile with datasource, Redis, MyBatis Plus, Flyway, and Resilience4j defaults. Classified schema init SQL created per D-39.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize Vue Pure Admin Frontend Scaffold in achievement-web</name>
  <files>
    achievement-web/package.json,
    achievement-web/vite.config.ts,
    achievement-web/src/router/index.ts,
    achievement-web/src/store/index.ts,
    achievement-web/src/api/index.ts,
    achievement-web/src/App.vue,
    achievement-web/src/main.ts,
    achievement-web/index.html,
    achievement-web/tsconfig.json,
    achievement-web/tsconfig.node.json,
    achievement-web/env.d.ts,
    achievement-web/.env,
    achievement-web/.env.development,
    achievement-web/src/style/index.scss,
    achievement-web/src/layout/index.vue
  </files>
  <read_first>
    CONTEXT.md D-05 (Vue Pure Admin template), D-36 (6 sub-menus under 系统管理), D-37 (welcome dashboard as homepage)
    UI-SPEC.md: Design System section, P-10 Navigation Menu (layout sidebar 220px, navbar 48px, page padding 16px)
    CLAUDE.md: Vue 3.4+, Element Plus 2.9+, Vite 5, Pinia 3.x, TypeScript
  </read_first>
  <action>
    Create a Vue 3 + Element Plus + Vue Pure Admin based SPA in the `achievement-web/` directory.

    1. Create `achievement-web/package.json` with:
       - name: `achievement-web`, version: `0.1.0`, private: true
       - type: `module`
       - Scripts: `dev: "vite"`, `build: "vue-tsc --noEmit && vite build"`, `preview: "vite preview"`
       - Dependencies: `vue@^3.4.0`, `vue-router@^4.3.0`, `pinia@^3.0.0`, `element-plus@^2.9.0`, `@element-plus/icons-vue`, `axios`, `dayjs`, `nprogress`
       - DevDependencies: `vite@^5.4.0`, `@vitejs/plugin-vue`, `typescript@^5.4.0`, `vue-tsc@^2.0.0`, `sass`, `unplugin-auto-import`, `unplugin-vue-components`

    2. Create `achievement-web/vite.config.ts`:
       - Vue plugin via `@vitejs/plugin-vue`
       - Auto-import: Element Plus resolvers (unplugin-vue-components)
       - Server proxy: `/api` -> `http://localhost:8080`
       - Build output: `dist/`
       - Resolve alias: `@` -> `./src`

    3. Create `achievement-web/index.html`:
       - Minimal HTML5 template, `#app` mount, charset utf-8, viewport meta
       - Title: "科研成果与知识产权管理系统"
       - No external fonts (use system font stack per UI-SPEC)

    4. Create TypeScript config files:
       - `tsconfig.json`: strict mode, target ES2022, moduleResolution bundler, paths `@/*` -> `./src/*`
       - `tsconfig.node.json`: for vite.config.ts
       - `env.d.ts`: declare module `*.vue`, `*.scss`

    5. Create `achievement-web/.env.development`:
       - `VITE_API_BASE_URL=/api`

    6. Create `achievement-web/src/main.ts`:
       - Import vue, createApp, use Pinia, use Element Plus, use Router
       - Import global styles from `@/style/index.scss`
       - Import `nprogress/nprogress.css`
       - Mount to `#app`

    7. Create `achievement-web/src/style/index.scss`:
       - CSS custom properties for the design system per UI-SPEC:
         - `--el-color-primary: #409EFF`
         - `--el-color-success: #67C23A`
         - `--el-color-warning: #E6A23C`
         - `--el-color-danger: #F56C6C`
         - `--el-color-info: #909399`
       - Page background: `#F5F7FA`
       - Body font-family: system font stack from UI-SPEC
       - `* { box-sizing: border-box; }`
       - `html, body, #app { height: 100%; margin: 0; }`

    8. Create `achievement-web/src/router/index.ts`:
       - Vue Router with createRouter, createWebHashHistory
       - Routes placeholder for:
         - `/dashboard` -> (placeholder component, lazy import `@/views/dashboard/index.vue`)
         - `/profile` -> (placeholder component, lazy import `@/views/profile/index.vue`)
         - `/system/user` -> (placeholder component)
         - `/system/role` -> (placeholder component)
         - `/system/department` -> (placeholder component)
         - `/system/dict` -> (placeholder component)
         - `/system/audit-log` -> (placeholder component)
         - `/system/api-config` -> (placeholder component)
         - `/login` -> (placeholder component)
         - `/` redirect to `/dashboard`

    9. Create `achievement-web/src/api/index.ts`:
       - Axios instance with baseURL from `import.meta.env.VITE_API_BASE_URL`
       - Request interceptor: attach Authorization Bearer token from pinia store
       - Response interceptor: unwrap response.data, handle 401 -> redirect to /login, handle global errors

    10. Create `achievement-web/src/store/index.ts`:
        - Pinia instance, export `useStore()`
        - Create `src/store/user.ts` with Pinia options store: token, roles, permissions, userInfo
        - Create `src/store/app.ts` with Pinia options store: sidebar collapsed state, menu list

    11. Create `achievement-web/src/layout/index.vue`:
        - Vue Pure Admin-style layout shell:
          - Left sidebar (ElMenu, 220px expanded, collapsible to 64px)
          - Top navbar (48px, breadcrumb + user avatar dropdown + logout)
          - Main content area with `<router-view />`
          - Sidebar renders menu items from pinia store
        - Menu structure per UI-SPEC P-10 (under 系统管理):
          - 系统管理 (ElSubMenu, icon Setting)
            - 用户管理 -> /system/user
            - 角色管理 -> /system/role
            - 部门管理 -> /system/department
            - 数据字典 -> /system/dict
            - 审计日志 -> /system/audit-log
            - API集成配置 -> /system/api-config
        - Top routes: 首页 (/dashboard, icon HomeFilled), 个人中心 in dropdown
        - Style: dark sidebar #1d1e1f, white navbar and content area per UI-SPEC Design System

    12. Create placeholder Vue view files:
        - `src/views/dashboard/index.vue`: welcome page placeholder with title "欢迎回来，{username}"
        - `src/views/profile/index.vue`: profile page placeholder
        - `src/views/login/index.vue`: login page placeholder (minimal, centered card, no branding per D-38)
        - `src/views/system/user/index.vue`: user management placeholder
        - `src/views/system/role/index.vue`: role management placeholder
        - `src/views/system/department/index.vue`: department management placeholder
        - `src/views/system/dict/index.vue`: data dictionary placeholder
        - `src/views/system/audit-log/index.vue`: audit log placeholder
        - `src/views/system/api-config/index.vue`: API config placeholder

    13. Create `achievement-web/src/App.vue`:
        - Import layout component
        - `<router-view />` inside layout
        - If route is /login, render without layout (full-screen standalone page)

    Do NOT install npm packages (npm install) in this task -- that is a verification step. Write all files via the Write tool.
  </action>
  <verify>
    <automated>cd achievement-web && npm install 2>&1 | tail -5 && npx vue-tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    1. `npm install` installs without errors in achievement-web
    2. `npx vue-tsc --noEmit` exits with exit code 0 (no TypeScript errors)
    3. `vite.config.ts` has proxy `/api` -> `http://localhost:8080`
    4. `src/router/index.ts` has all routes listed (dashboard, login, profile, 6 system management routes)
    5. `src/layout/index.vue` has sidebar with 系统管理 -> 6 sub-menu items per D-36
    6. `src/store/user.ts` exported with token, roles, permissions, userInfo
    7. `src/api/index.ts` has Axios interceptor for Authorization Bearer token
    8. `index.html` title is "科研成果与知识产权管理系统"
  </acceptance_criteria>
  <done>Vue 3 + Element Plus + Vue Pure Admin scaffold created. All route placeholders exist. Layout shell with sidebar navigation matching D-36. Axios with interceptors. Pinia stores for user/app state. npm install success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker containers <-> Host | MySQL 3306 and Redis 6379 exposed to host network |
| dev profile config | Credentials (DB password, Redis password) in plaintext YAML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure | application-dev.yml | accept | Dev-only file with local credentials; in production these would use environment variables |
| T-00-01-SC | Tampering | npm install (element-plus, vue-router, axios, etc.) | mitigate | All packages are official npm registry packages -- run `npm audit` after install. No [SUS] or [SLOP] packages identified. |
| T-00-01-02 | Elevation of Privilege | Docker named volumes | accept | Dev-only, no sensitive data in Phase 0 |
</threat_model>

<verification>
1. `./mvnw clean install -DskipTests` succeeds
2. `docker compose config` validates
3. `cd achievement-web && npm install && npx vue-tsc --noEmit` succeeds
4. Project directory structure shows all 4+1 modules
5. `.gitignore` covers common temp/build artifacts
</verification>

<success_criteria>
- Maven multi-module project compiles with Spring Boot 4.1 + JDK 21
- Docker Compose defines MySQL 8.4 + Redis 7.x with named volumes and healthchecks
- Vue Pure Admin scaffold compiles with TypeScript strict mode
- All 6 system management route placeholders created in router
- Layout shell renders with sidebar (220px) + navbar (48px) showing system management menu
</success_criteria>

<output>
Create `.planning/phases/00-foundation-infrastructure/00-01-SUMMARY.md` when done
</output>
