---
phase: 01
plan: 05
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  # Backend — Invalidation
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/service/InvalidationService.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/entity/InvalidationRecord.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/mapper/InvalidationRecordMapper.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/controller/InvalidationController.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/dto/InvalidationDTO.java
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/dto/InvalidationVO.java
  # Backend — Classified data isolation (enhancement)
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/service/ClassifiedPermissionService.java
  # Backend — Duplicate detection (enhancement)
  - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/service/DuplicateCheckService.java
  # Backend — Achievement service (add invalidation actions to detail endpoint)
  - achievement-module/achievement-paper/src/main/java/com/institute/achievement/paper/service/PaperService.java
  - achievement-module/achievement-patent/src/main/java/com/institute/achievement/patent/service/PatentService.java
  - achievement-module/achievement-copyright/src/main/java/com/institute/achievement/copyright/service/CopyrightService.java
  # Database
  - achievement-module/achievement-system/src/main/resources/db/migration/V10__create_invalidation_record_table.sql
  # Frontend — DuplicateDialog
  - achievement-web/src/components/achievement/DuplicateDialog.vue
  # Frontend — Detail page (add invalidate action, duplicate dialog integration)
  - achievement-web/src/views/achievement/AchievementDetail.vue
  # Frontend — Register page (add submit-time duplicate check integration)
  - achievement-web/src/views/achievement/AchievementRegister.vue
  # Frontend — Classified marking display
  - achievement-web/src/components/achievement/ClassifiedTag.vue
  # Frontend — API
  - achievement-web/src/api/achievement/invalidation.ts
  # Tests
  - achievement-module/achievement-system/src/test/java/com/institute/achievement/system/InvalidationServiceTest.java
  - achievement-module/achievement-system/src/test/java/com/institute/achievement/system/ClassifiedPermissionServiceTest.java
  - achievement-module/achievement-system/src/test/java/com/institute/achievement/system/DuplicateCheckServiceTest.java
autonomous: true
requirements:
  - REG-08
  - REG-09
  - REG-10
must_haves:
  truths:
    - "已归档成果可以被创建人或科研秘书作废，作废后仅创建人和系统管理员可见"
    - "作废操作不可撤销，作废原因和操作记录在审计日志中留存"
    - "提交成果时，系统检测同一DOI/申请号是否已存在，重复时弹窗提示"
    - "弹窗显示已存在成果的基本信息，用户可选择查看已有成果或继续填写"
    - "涉密成果标记后在成果列表和详情页显示密级标签"
    - "涉密成果的附件仅授权人员（涉密成果管理员和创建人）可下载"
    - "草稿提交时不检测重复"
  artifacts:
    - path: "achievement-module/achievement-system/src/main/java/.../system/service/InvalidationService.java"
      provides: "Achievement invalidation with reason logging and visibility control"
      exports: ["invalidate", "getInvalidationRecords", "isVisibleToUser"]
      min_lines: 80
    - path: "achievement-module/achievement-system/src/main/java/.../system/service/DuplicateCheckService.java"
      provides: "Unified duplicate detection for all achievement types"
      exports: ["checkDuplicate", "checkDoi", "checkApplicationNo", "checkRegistrationNo", "findExistingByDoi", "findExistingByApplicationNo", "findExistingByRegistrationNo"]
      min_lines: 50
    - path: "achievement-web/src/components/achievement/DuplicateDialog.vue"
      provides: "Duplicate detection dialog showing existing achievement info"
      contains: "ElDialog with achievement summary, view-existing and continue buttons"
  key_links:
    - from: "AchievementDetail.vue (action bar)"
      to: "InvalidationService.java"
      via: "POST /api/achievement/invalidate"
      pattern: "invalidationApi\\.invalidate"
    - from: "DuplicateDialog.vue"
      to: "AchievementDetail.vue"
      via: "[查看已有] button navigates to /achievement/detail/{existingId}"
      pattern: "router\\.push.*achievement/detail"
    - from: "DuplicateCheckService.java"
      to: "PaperMapper / PatentMapper / CopyrightMapper"
      via: "Queries by doi/application_no/registration_no"
      pattern: "selectOne.*eq\\(.*doi"

---

<objective>
Phase 1 Plan 5: Implement achievement invalidation with reason logging, classified marking with visual labels and access control, and submit-time duplicate detection with preview dialog.

Purpose: Complete the achievement lifecycle management by adding invalidation (end-of-life), classified information security (access control), and data integrity protection (duplicate prevention). This plan polishes and secures the registration and management flows created in Plans 01-01 through 01-03.

Output: Researchers can invalidate their own archived achievements with reason recording; classified achievements display visual security labels and restrict attachment downloads; duplicate submissions are caught at submit time with a user-friendly dialog showing the existing record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
<!--
Depends on achievements (Plan 01-01, 01-02) and approval system (Plan 01-03).

Key decision references (CONTEXT.md):
    D-34: Direct invalidation — no approval needed, creator or secretary can invalidate
    D-35: After invalidation — only creator and admin can see
    D-36: Invalidation is irreversible
    D-45: Submit-time duplicate check (not real-time)
    D-46: Dialog on detect, can view existing achievement
    D-47: Drafts skip duplicate check
    D-06: Classified marking switch
    D-10: Classified attachment access control
    D-39: Detail page uses tabs (basic info / approval progress / attachments / operation log)
    D-40: Top action bar, dynamic by status

Status values from AchievementStatusEnum:
    - ARCHIVED: can be invalidated
    - INVALIDATED: view-only, hidden from non-creator/admin

Classified levels:
    - 秘密 (secret) — Orange tag
    - 机密 (confidential) — Red tag

Duplicate detection rules:
    - Paper: DOI uniqueness (allow null DOI, check only non-null)
    - Patent: application_no uniqueness (required field)
    - Copyright: registration_no uniqueness (required field)
-->

From UI-SPEC §5.2 (Invalidation dialog):
    title="确认作废", message="确认作废该成果？此操作不可撤销。作废后仅创建人和系统管理员可见。", confirm="确认作废"

From UI-SPEC §6.9 (DuplicateDialog):
    Title: "重复提示"
    Shows: existing achievement title, type, status, submit time
    Actions: [查看已有成果] [继续填写并提交]

From UI-SPEC §4.4 (Classified marking colors):
    秘密: Orange (#E6A23C)
    机密: Red (#F56C6C)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — InvalidationService, InvalidationRecord, enhanced DuplicateCheckService, classified permission integration</name>
  <files>
    - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/service/InvalidationService.java
    - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/entity/InvalidationRecord.java
    - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/mapper/InvalidationRecordMapper.java
    - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/controller/InvalidationController.java
    - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/dto/InvalidationDTO.java
    - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/dto/InvalidationVO.java
    - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/service/DuplicateCheckService.java
    - achievement-module/achievement-system/src/main/java/com/institute/achievement/system/service/ClassifiedPermissionService.java
    - achievement-module/achievement-system/src/main/resources/db/migration/V10__create_invalidation_record_table.sql
    - achievement-module/achievement-system/src/test/java/com/institute/achievement/system/InvalidationServiceTest.java
    - achievement-module/achievement-system/src/test/java/com/institute/achievement/system/ClassifiedPermissionServiceTest.java
    - achievement-module/achievement-system/src/test/java/com/institute/achievement/system/DuplicateCheckServiceTest.java
  </files>
  <read_first>
    - CONTEXT.md lines 69-72 (D-34~D-36 invalidation), lines 86-89 (D-45~D-47 duplicate detection)
    - UI-SPEC.md §6.9 (DuplicateDialog), UI-SPEC.md §4.4 (Classified colors)
    - RESEARCH.md lines 519-558 (AchievementStatusEnum — INVALIDATED status)
    - Plan 01-03 ApprovalService.java (ApprovalAction.INVALIDATE transition)
    - Plan 01-04 ClassifiedPermissionService.java (base version to enhance)
  </read_first>
  <action>
    Create invalidation backend, enhance duplicate check service with find-existing methods, and fully integrate classified permission controls.

    **Migration V10 — InvalidationRecord table:**
    CREATE TABLE `invalidation_record` (
      id BIGINT AUTO_INCREMENT PRIMARY KEY,
      achievement_type VARCHAR(50) NOT NULL COMMENT 'paper/patent/copyright',
      achievement_id BIGINT NOT NULL,
      invalidator_id BIGINT NOT NULL,
      invalidator_name VARCHAR(100) NOT NULL,
      reason TEXT NOT NULL COMMENT '作废原因',
      created_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
      INDEX idx_achievement (achievement_type, achievement_id),
      INDEX idx_invalidator (invalidator_id)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

    **InvalidationRecord entity:** @TableName("invalidation_record"), @Data, fields match migration.
    **InvalidationRecordMapper:** extends BaseMapper, findByAchievement(String type, Long id).
    **InvalidationDTO:** achievementType, achievementId, reason (@NotBlank, max 500).
    **InvalidationVO:** achievementType, achievementId, invalidatorName, reason, createdTime.

    **InvalidationService (per D-34~D-36):**
    - @Service, @Transactional
    - invalidate(InvalidationDTO dto, Long userId):
      - Load achievement by type+id, validate status == ARCHIVED
      - Validate permission: userId == created_by OR user has secretary role in same department (D-34)
      - Transition status to INVALIDATED
      - Create InvalidationRecord with reason
      - AuditLogService.log("INVALIDATE", achievementId, userId, "作废: " + reason)
      - Notify system admin
    - isVisibleToUser(type, id, userId):
      - If status != INVALIDATED: return true
      - If status == INVALIDATED: return userId == created_by OR user has SYSTEM_ADMIN role (D-35)
    - getAllInvalidations(userId, isAdmin): admin sees all, others see own

    **InvalidationController:**
    - POST /api/achievement/invalidate
    - GET /api/achievement/invalidation?type={type}&id={id}
    - GET /api/achievement/invalidations (list for current user)

    **DuplicateCheckService — enhance (per D-45~D-47):**
    - Add findExistingByDoi(String doi): return Paper with same doi and status != INVALIDATED
    - Add findExistingByApplicationNo(String applicationNo): return Patent
    - Add findExistingByRegistrationNo(String registrationNo): return Copyright
    - Add checkDuplicateForSubmit(String achievementType, String uniqueField): returns DuplicateCheckResult
    - DuplicateCheckResult: @Data, fields: boolean duplicate, Long existingId, String existingTitle, String existingType, String existingStatus (Chinese label), String existingSubmitTime
    - Draft: return duplicate=false for drafts (D-47: skip duplicate check for drafts)

    **ClassifiedPermissionService — enhance (per REG-10):**
    - filterClassifiedAchievements(List<Long> ids, String type, Long userId): returns visible-only filtered list
    - canUserViewAttachment(Long attachmentId, Long userId): enhanced with classified achievement check
    - getUserClassifiedRole(Long userId): returns role info
    - Integration: PaperService/PatentService/CopyrightService page queries call filterClassifiedAchievements; getById calls isVisibleToUser

    **Tests:**
    - InvalidationServiceTest: testInvalidateArchivedPaper (full cycle), testInvalidateByCreator, testInvalidateBySecretary, testInvalidateNonArchived (error), testInvalidateByUnauthorized (error), testInvalidationIrreversible, testVisibilityAfterInvalidation (D-35), testInvalidateAllTypes
    - DuplicateCheckServiceTest: testFindExistingByDoi, testFindExistingByApplicationNo, testFindExistingByRegistrationNo, testCheckDuplicateForSubmit (returns proper DuplicateCheckResult), testNoDuplicateForDraft (D-47)
    - ClassifiedPermissionServiceTest: testCanViewClassified (creator yes, manager yes, other no), testCanDownloadClassifiedAttachment, testFilterClassifiedAchievements, testNonClassifiedAlwaysVisible
  </action>
  <verify>
    <automated>mvn test -pl achievement-module/achievement-system -Dtest=InvalidationServiceTest,DuplicateCheckServiceTest,ClassifiedPermissionServiceTest -DskipITs</automated>
  </verify>
  <acceptance_criteria>
    1. InvalidationService.invalidate transitions ARCHIVED -> INVALIDATED, creates InvalidationRecord with reason
    2. Only creator or dept secretary can invalidate; others get error
    3. Non-archived achievements cannot be invalidated
    4. INVALIDATED achievements are hidden from non-creator/admin users (D-35)
    5. DuplicateCheckService.findExistingByDoi returns existing paper with matching DOI
    6. DuplicateCheckService.checkDuplicateForSubmit returns DuplicateCheckResult with all required fields
    7. Draft submissions skip duplicate check (D-47)
    8. ClassifiedPermissionService.filterClassifiedAchievements removes unauthorized classified items
    9. Classified attachment download blocked for unauthorized users
    10. All unit tests pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Frontend — DuplicateDialog, invalidation flow in detail page, classified tag, register page duplicate check</name>
  <files>
    - achievement-web/src/components/achievement/DuplicateDialog.vue
    - achievement-web/src/views/achievement/AchievementDetail.vue
    - achievement-web/src/views/achievement/AchievementRegister.vue
    - achievement-web/src/components/achievement/ClassifiedTag.vue
    - achievement-web/src/api/achievement/invalidation.ts
  </files>
  <read_first>
    - UI-SPEC.md §6.9 (DuplicateDialog screen spec), §5.2 (Invalidation dialog text)
    - UI-SPEC.md §5.3 (Empty states), §4.4 (Classified marking colors)
    - Plan 01-01 AchievementRegister.vue (submit flow — inject duplicate check before actual submit)
    - Plan 01-03 AchievementDetail.vue (action bar — add invalidate button for ARCHIVED status)
    - UI-SPEC.md §12.1 (Pinia store for achievement — extend with duplicate check action)
  </read_first>
  <action>
    Create the frontend components for duplicate detection, invalidation, and classified marking.

    **API layer (api/achievement/invalidation.ts):**
    - invalidate(type, id, reason) -> POST /api/achievement/invalidate
    - getInvalidationRecord(type, id) -> GET /api/achievement/invalidation
    - getInvalidations() -> GET /api/achievement/invalidations
    - checkDuplicate(type, uniqueField) -> GET /api/achievement/check-duplicate (or use existing DuplicateCheckService via PaperService)

    **DuplicateDialog.vue (per UI-SPEC §6.9):**
    - Props: visible (boolean), duplicateData (DuplicateCheckResult)
    - el-dialog, title: "重复提示", width: 500px
    - Warning icon (el-icon-warning) at top
    - Content section with existing achievement summary:
      - el-descriptions: 标题, 类型, 状态, 提交时间
      - Data from duplicateData prop
    - Message: "您正在登记的成果与上述成果{DOI/申请号}相同。"
    - Actions:
      - [查看已有成果] el-button -> emit 'view-existing' with existingId, parent navigates to /achievement/detail/{existingId} per D-46
      - [继续填写并提交] el-button type="primary" -> emit 'continue-submit', parent proceeds with submission
    - Close on overlay click: false (must choose action per D-46)

    **AchievementRegister.vue — add duplicate check before submit:**
    - On submit click (before API call):
      - Step 1: Validate form fields
      - Step 2: If activeType has unique identifier (doi for paper, applicationNo for patent, registrationNo for copyright):
        - Call DuplicateCheckService check API
        - If duplicate found: show DuplicateDialog instead of submitting
        - If user clicks [查看已有成果]: navigate to existing achievement detail
        - If user clicks [继续填写并提交]: proceed with submit (D-46 allows continuing despite duplicate)
      - Step 3: Submit API call
    - For drafts, skip duplicate check entirely (D-47)
    - DOI is optional for paper (if empty, skip duplicate check for DOI)

    **ClassifiedTag.vue (per UI-SPEC §4.4):**
    - Props: level (String: 'secret' | 'confidential')
    - If secret: el-tag type="warning" (orange), label "秘密"
    - If confidential: el-tag type="danger" (red), label "机密"
    - Small size, used inline next to achievement title or status
    - Used in: AchievementList (type column), AchievementDetail (title area), ApprovalDetail (left panel)

    **AchievementDetail.vue — add invalidation action:**
    - In action bar, for ARCHIVED status:
      - [作废] el-button type="danger", shown only if user == creator OR user has secretary role (D-34)
      - On click: show ElMessageBox.confirm with UI-SPEC §5.2 text
        - title="确认作废", message="确认作废该成果？此操作不可撤销。作废后仅创建人和系统管理员可见。"
      - On confirm: call invalidationApi.invalidate(type, id, reason)
        - Reason input in the dialog (el-input type="textarea")
      - On success: ElMessage.success "成果已作废", refresh detail page (status changes to INVALIDATED)
    - For INVALIDATED status:
      - Action bar: no actions shown (view only)
      - Info banner at top: "该成果已作废，仅创建人和系统管理员可见" (el-alert type="warning")
      - Disable edit/download actions

    **Integration of ClassifiedTag into existing pages:**
    - AchievementList.vue: add column for 密级, conditionally show ClassifiedTag if achievement is classified
    - AchievementDetail.vue: show ClassifiedTag next to title, above basic info tab
    - ApprovalDetail.vue (left panel): show ClassifiedTag next to title

    **No additional store changes needed** — achievement store already supports isClassified/classifiedLevel fields. Invalidation state is reflected by status=INVALIDATED.
  </action>
  <verify>
    <automated>grep -c "DuplicateDialog\|ClassifiedTag\|invalidate" achievement-web/src/views/achievement/AchievementRegister.vue achievement-web/src/views/achievement/AchievementDetail.vue</automated>
  </verify>
  <acceptance_criteria>
    1. DuplicateDialog shows existing achievement info with [查看已有成果] and [继续填写并提交] buttons
    2. Register page checks duplicate before submit (D-45), shows DuplicateDialog if found
    3. Draft submissions skip duplicate check (D-47)
    4. Detail page shows [作废] button for ARCHIVED status when user has permission (D-34)
    5. Invalidation dialog has reason textarea, confirm shows success toast, page refreshes
    6. INVALIDATED achievements show info banner, no action buttons, hidden from non-creator/admin
    7. ClassifiedTag renders with correct color per level (橙色=秘密, 红色=机密)
    8. ClassifiedTag appears in list, detail, and approval pages for classified achievements
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integration test — full invalidation flow, duplicate detection across all types, classified access enforcement</name>
  <files>
    - achievement-module/achievement-system/src/test/java/com/institute/achievement/system/AchievementLifecycleIntegrationTest.java
  </files>
  <read_first>
    - Plan 01-03 Task 3 integration test pattern
    - Completion of Task 1 and Task 2 above
  </read_first>
  <action>
    Create integration test covering invalidation, duplicate detection, and classified access enforcement.

    **AchievementLifecycleIntegrationTest:**
    - @SpringBootTest with @AutoConfigureMockMvc
    - Test users: researcher (creator), dept secretary, admin, other-department user, classified manager

    - testFullLifecycle_Paper:
      - Create -> submit -> dept approve -> admin archive -> verify ARCHIVED
      - Invalidated by creator -> verify INVALIDATED + invalidation record + audit log
      - Creator views -> visible (200)
      - Other user views -> hidden (403 or filtered from list)
      - Admin views -> visible (200 per D-35)

    - testDuplicateDetection_Doi:
      - Create paper with DOI=X -> submit -> status PENDING_DEPT_REVIEW
      - Create paper with DOI=X -> submit -> verify duplicate check returns existing achievement info
      - Verify DuplicateCheckResult has non-null existingId, title, type, status, time
      - Verify user can choose to continue despite duplicate

    - testDuplicateDetection_ApplicationNo:
      - Same flow for patent with application_no

    - testDuplicateDetection_RegistrationNo:
      - Same flow for copyright with registration_no

    - testDuplicateDetection_DraftSkipped:
      - Create draft with DOI=X (status=DRAFT)
      - Create new paper with DOI=X -> submit -> duplicate SHOULD be detected (draft itself counts)
      - The draft is saved but the submit check finds it

    - testClassifiedAccess_Paper:
      - Create paper with isClassified=true, level=秘密
      - Non-classified user queries list -> paper not visible (filtered out)
      - Creator queries list -> paper visible
      - Classified manager queries list -> paper visible
      - Non-classified user tries to download attachment -> 403

    - testClassifiedAccess_Patent:
      - Same flow for classified patent

    - testInvalidation_CreatorOnly:
      - Create paper as user A -> archive -> user B (same dept, non-secretary) tries to invalidate -> 403
      - User A (creator) invalidates -> 200
      - Try to invalidate already invalidated -> 400 (irreversible)

    - testInvalidation_SecretaryCanAct:
      - Create paper as user A -> archive -> dept secretary (same dept) invalidates -> 200

    **E2E smoke test documentation (in code comments):**
    - Full lifecycle: Create -> Submit -> Approve -> Archive -> Invalidate -> Verify hidden
    - Duplicate: Create -> Submit duplicate -> See dialog -> View existing -> Continue
    - Classified: Create classified -> Verify visibility rules apply
  </action>
  <verify>
    <automated>mvn test -pl achievement-module/achievement-system -Dtest=AchievementLifecycleIntegrationTest -DskipITs=false</automated>
  </verify>
  <acceptance_criteria>
    1. Full lifecycle (create -> submit -> approve -> archive -> invalidate) works end-to-end for all types
    2. Duplicate detection returns correct existing achievement info at submit time
    3. Duplicate detection works for DOI (paper), application_no (patent), registration_no (copyright)
    4. Draft skip rule: drafts are checked for duplicate when submitted (the existing draft is a duplicate)
    5. Classified achievements filtered from non-authorized user's list view
    6. Classified attachment download blocked for unauthorized users
    7. Only creator and dept secretary can invalidate archived achievements
    8. Invalidation is irreversible (cannot invalidate already invalidated)
    9. INVALIDATED achievements visible to creator and admin, hidden from others
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invalidation API (browser -> server) | Authorized user destroys achievement visibility |
| duplicate check API (browser -> server) | User-submitted DOI/application_no/registration_no checked against DB |
| classified data access (server -> DB) | Classified achievement rows fetched with permission filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Denial of Service | Invalidation abused to hide achievements | mitigate | Only creator and dept secretary can invalidate; audit log records every invalidation with user ID and reason |
| T-01-17 | Information Disclosure | Classified achievement data leaked to unauthorized user | mitigate | ClassifiedPermissionService.filterClassifiedAchievements in page queries + isVisibleToUser in getById; both enforced at service layer |
| T-01-18 | Information Disclosure | Classified attachment downloaded by unauthorized user | mitigate | AttachmentService.download calls ClassifiedPermissionService.canUserViewAttachment before streaming |
| T-01-19 | Tampering | Duplicate detection bypassed via direct API call | mitigate | DuplicateCheckService.checkDuplicateForSubmit called inside PaperService.submitPaper, not just in frontend — server-side enforcement per D-45 |
| T-01-20 | Repudiation | Invalidation without trace | mitigate | InvalidationRecord created + AuditLogService.logged in same @Transactional block |
| T-01-SC | Tampering | npm/Maven package compromise | mitigate | All packages verified in RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
1. `mvn test -pl achievement-module/achievement-system -Dtest=InvalidationServiceTest,DuplicateCheckServiceTest,ClassifiedPermissionServiceTest -DskipITs` passes
2. `mvn test -pl achievement-module/achievement-system -Dtest=AchievementLifecycleIntegrationTest` passes
3. Manual verification: Create paper -> archive -> invalidate -> verify hidden from other users -> verify visible to creator and admin
4. Manual verification: Submit paper with existing DOI -> verify duplicate dialog appears -> verify can view existing or continue
5. Manual verification: Create classified paper -> verify tag shows in list/detail -> verify non-authorized user cannot see it
</verification>

<success_criteria>
1. InvalidationService invalidates archived achievements, records reason, logs audit, enforces permission
2. INVALIDATED achievements hidden from non-creator/admin users (D-35)
3. DuplicateCheckService checks DOI/applicationNo/registrationNo uniqueness at submit time
4. Duplicate check returns existing achievement details for dialog display (D-46)
5. Draft submission skips duplicate check (D-47)
6. ClassifiedPermissionService filters unauthorized users from seeing classified achievements
7. Classified attachment download is blocked for unauthorized users
8. ClassifiedTag.vue renders correct color per classification level
9. Integration tests verify full lifecycle, duplicate detection, and classified access enforcement
10. All decisions D-34~D-36, D-45~D-47, and REG-08~REG-10 are implemented per spec
</success_criteria>

<output>
Create `.planning/phases/01-achievement-registration-approval/01-05-SUMMARY.md` when done.
</output>
