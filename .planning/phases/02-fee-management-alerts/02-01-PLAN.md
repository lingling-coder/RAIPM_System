---
phase: 02-fee-management-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - achievement-module/pom.xml
  - achievement-module/achievement-fee/pom.xml
  - achievement-common/src/main/java/com/institute/achievement/common/event/AchievementArchivedEvent.java
  - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/entity/FeeRecord.java
  - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/enums/FeeTypeEnum.java
  - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/enums/FeeStatusEnum.java
  - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/dto/FeeRecordDTO.java
  - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/dto/FeeRecordVO.java
  - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/mapper/FeeRecordMapper.java
  - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/service/FeeRecordService.java
  - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/service/impl/FeeRecordServiceImpl.java
  - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/controller/FeeRecordController.java
  - achievement-module/achievement-fee/src/main/resources/db/migration/V11__create_fee_record_table.sql
  - achievement-web/src/router/index.ts
  - achievement-web/src/views/fee/FeeManagement.vue
  - achievement-web/src/views/fee/FeeLedger.vue
  - achievement-web/src/views/fee/FeeDetail.vue
  - achievement-web/src/api/fee/feeRecord.ts
autonomous: true
requirements: [FEE-01]
user_setup: []

must_haves:
  truths:
    - "User can view patent/copyright fee records in paginated table"
    - "User can filter fee records by status (pending/paid/paused) and quick-filter (即将逾期/已逾期/本月需缴费/全部)"
    - "User can view fee record detail with tabs (基本信息/缴费历史/附件/操作日志)"
    - "Fee records are associated with patents and copyrights via owner_type + owner_id"
    - "Funding source is configured via data dictionary (纵向科研经费/横向科研经费/院配套/自筹)"
  artifacts:
    - path: "achievement-module/achievement-fee/pom.xml"
      provides: "New fee module Maven artifact definition"
    - path: "achievement-module/achievement-fee/src/main/resources/db/migration/V11__create_fee_record_table.sql"
      provides: "Fee_record table with all D-13 fields"
    - path: "achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/controller/FeeRecordController.java"
      provides: "/api/fees CRUD + paginated list + filter endpoints"
    - path: "achievement-web/src/views/fee/FeeLedger.vue"
      provides: "Fee record list view with el-table, quick filters, pagination"
    - path: "achievement-web/src/router/index.ts"
      provides: "/fee route with FeeManagement wrapper and 3 sub-tabs"
  key_links:
    - from: "FeeRecordController"
      to: "FeeRecordService"
      via: "Spring DI"
      pattern: "private final FeeRecordService feeRecordService"
    - from: "FeeRecordService"
      to: "FeeRecordMapper"
      via: "MyBatis-Plus BaseMapper"
      pattern: "extends BaseMapper<FeeRecord>"
    - from: "FeeRecord"
      to: "Patent"
      via: "owner_type='patent' + owner_id=Patent.id"
      pattern: "ownerType/ownerId"
    - from: "FeeLedger.vue"
      to: "/api/fees/page"
      via: "GET /api/fees/page"
      pattern: "feeRecordApi.getPage"
---

<objective>
**Plan 02-01: Fee Ledger Foundation — Module Structure + Fee Record CRUD**

This plan creates the `achievement-fee` sub-module and delivers the core fee ledger: database table, backend CRUD + paginated listing with multi-dimensional filtering, and the frontend Fee Ledger tab under the /fee menu. This is the foundation that all subsequent Phase 2 plans build on.

**Purpose:** Establish the fee management data model, module structure, and basic ledger view so users can see and search fee records associated with patents/copyrights.

**Outputs:**
- New `achievement-fee` Maven sub-module registered in achievement-module/pom.xml
- `fee_record` database table with all fields per D-13
- FeeRecord entity, enums (FeeTypeEnum, FeeStatusEnum), DTO/VO, Mapper, Service, Controller
- Full CRUD + paginated listing with filtering by status, due_date range, fee_type, funding_source
- Frontend: /fee route in router, FeeManagement.vue wrapper with 3 tab slots, FeeLedger.vue table view, FeeDetail.vue detail page with tabs
- Frontend API service: feeRecord.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-fee-management-alerts/02-CONTEXT.md
@.planning/phases/02-fee-management-alerts/02-RESEARCH.md

<interfaces>
<!-- Key types the executor will need for context -->

From Patent entity:
```java
// achievement-module/achievement-patent/.../entity/Patent.java
public class Patent {
    private Long id;
    private String patentName;
    private String inventors;
    private String applicationNo;
    private LocalDate authorizationDate;    // Used for fee plan due date calc
    private LocalDate nextFeeDate;          // First fee due date
    private String legalStatus;
    private String patentType;
    private Long deptId;
    private Long createdBy;
}
```

From NotificationService.send():
```java
// achievement-module/achievement-system/.../service/NotificationService.java
@Transactional
public Long send(Long userId, String type, String title, String content,
                 String relatedAchievementType, Long relatedAchievementId)
// type values: "APPROVAL", "SYSTEM" — will add "ALERT" in 02-03
```

From Result wrapper:
```java
// achievement-common/.../util/Result.java
public class Result<T> {
    public static <T> Result<T> success(T data);
    public static <T> Result<T> success();
    public static <T> Result<T> error(String message);
}
```

From SecurityUtils:
```java
// achievement-framework/.../security/SecurityUtils.java
public static Long getCurrentUserId();
public static Long getCurrentDeptId();
public static List<String> getCurrentRoles();
```

Route pattern from router/index.ts:
```typescript
// achievement-web/src/router/index.ts
{
  path: '/fee',
  redirect: '/fee/ledger',
  meta: { title: '费用管理', icon: 'Money' },  // NEW top-level menu
  children: [
    { path: 'ledger', name: 'FeeLedger', component: () => import('@/views/fee/FeeLedger.vue'), meta: { title: '费用台账' } },
    { path: 'plan', name: 'FeePlan', component: () => import('@/views/fee/FeePlan.vue'), meta: { title: '缴费计划' } },
    { path: 'stats', name: 'FeeStats', component: () => import('@/views/fee/FeeStats.vue'), meta: { title: '费用统计' } },
  ]
}
```

Existing module pom.xml (patent module as template):
```xml
<!-- achievement-module/achievement-patent/pom.xml -->
<artifactId>achievement-patent</artifactId>
<packaging>jar</packaging>
<dependencies>
    <dependency><groupId>com.institute</groupId><artifactId>achievement-common</artifactId></dependency>
    <dependency><groupId>com.institute</groupId><artifactId>achievement-framework</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-web</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-validation</artifactId></dependency>
    <dependency><groupId>com.baomidou</groupId><artifactId>mybatis-plus-boot-starter</artifactId></dependency>
    <dependency><groupId>org.projectlombok</groupId><artifactId>lombok</artifactId><optional>true</optional></dependency>
    <dependency><groupId>org.mapstruct</groupId><artifactId>mapstruct</artifactId></dependency>
    <dependency><groupId>cn.hutool</groupId><artifactId>hutool-all</artifactId></dependency>
    <dependency><groupId>org.flywaydb</groupId><artifactId>flyway-core</artifactId><optional>true</optional></dependency>
    <!-- Test -->
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-test</artifactId><scope>test</scope></dependency>
    <dependency><groupId>com.h2database</groupId><artifactId>h2</artifactId><scope>test</scope></dependency>
</dependencies>
```

Existing migration pattern (V5 patent table):
```sql
-- V5__create_patent_table.sql
CREATE TABLE `patent` (
    `id`              BIGINT       NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    ...
    `next_fee_date`   DATE         NULL                    COMMENT '年费下次缴费日',
    ...
    PRIMARY KEY (`id`),
    INDEX `idx_status` (`status`),
    INDEX `idx_dept_id` (`dept_id`),
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
  COMMENT='专利成果表';

-- Note: fee_record table uses the same ENGINE/CHARSET/COLLATE pattern.
```
</interfaces>

**Key decisions (D-01 through D-13):**
- D-01: New `achievement-fee` sub-module under `achievement-module/`
- D-02: Frontend: top-level /fee menu with 3 sub-tabs (台账/计划/统计)
- D-03: Fee list uses standard el-table matching Phase 01 style
- D-04: Quick filter tags (即将逾期/已逾期/本月需缴费/全部) + advanced filters
- D-05: Inline operations: view + edit only (batch pay is separate flow)
- D-06: Detail page: 基本信息/缴费历史/附件/操作日志 tabs
- D-08: 经费来源 dropdown uses data dictionary FUND_SOURCE
- D-10: Arc polymorphic: owner_type + owner_id
- D-12: Fee types: annual_fee, registration_fee, maintenance_fee, other
- D-13: fee_record table fields specified exactly
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create achievement-fee module structure with fee_record database migration</name>
  <files>
    - achievement-module/pom.xml
    - achievement-module/achievement-fee/pom.xml
    - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/entity/FeeRecord.java
    - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/enums/FeeTypeEnum.java
    - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/enums/FeeStatusEnum.java
    - achievement-module/achievement-fee/src/main/resources/db/migration/V11__create_fee_record_table.sql
  </files>
  <action>
    Create the achievement-fee sub-module:

    1. Add `<module>achievement-fee</module>` to `achievement-module/pom.xml` in the modules list (alphabetically after achievement-copyright).

    2. Create `achievement-fee/pom.xml` following the exact pattern of `achievement-patent/pom.xml`:
       - parent: achievement-module (v0.1.0-SNAPSHOT)
       - artifactId: achievement-fee
       - dependencies: achievement-common, achievement-framework, spring-boot-starter-web, spring-boot-starter-validation, mybatis-plus-boot-starter, lombok, mapstruct, hutool-all, flyway-core (optional), spring-boot-starter-test (test), h2 (test)
       - Do NOT add achievement-patent as a compile dependency. The fee module interacts with Patent through Arc polymorphic owner_type/owner_id only.

    3. Create FeeTypeEnum in enums/ package:
       ```
       Values: ANNUAL_FEE("annual_fee", "专利年费"), REGISTRATION_FEE("registration_fee", "登记费"), MAINTENANCE_FEE("maintenance_fee", "维护费"), OTHER("other", "其他")
       Fields: code (String), label (String)
       Static: fromCode(String code) returning matching enum or null
       ```

    4. Create FeeStatusEnum in enums/ package:
       ```
       Values: PENDING("pending", "待缴费"), PAID("paid", "已缴费"), PAUSED("paused", "已暂停")
       Fields: code (String), label (String)
       Static: fromCode(String code) returning matching enum or null
       ```

    5. Create FeeRecord entity with @Data @TableName("fee_record"):
       - id (Long, @TableId AUTO)
       - feeType (String, @TableField("fee_type")) -- stores FeeTypeEnum code
       - amount (BigDecimal)
       - paidAmount (BigDecimal, @TableField("paid_amount"))
       - dueDate (LocalDate, @TableField("due_date"))
       - paidDate (LocalDate, @TableField("paid_date"))
       - voucherNo (String, @TableField("voucher_no"))
       - status (String) -- stores FeeStatusEnum code
       - fundingSource (String, @TableField("funding_source"))
       - ownerType (String, @TableField("owner_type"))
       - ownerId (Long, @TableField("owner_id"))
       - source (String) -- "auto_generated" or "manual"
       - slipNo (String, @TableField("slip_no")) -- nullable, filled during batch slip generation
       - slipGeneratedTime (LocalDateTime, @TableField("slip_generated_time")) -- nullable
       - slipGeneratedBy (Long, @TableField("slip_generated_by")) -- nullable
       - deptId (Long, @TableField("dept_id"))
       - createdBy (Long, @TableField("created_by"))
       - createdTime (LocalDateTime, @TableField(value = "created_time", fill = FieldFill.INSERT))
       - updatedBy (Long, @TableField("updated_by"))
       - updatedTime (LocalDateTime, @TableField(value = "updated_time", fill = FieldFill.UPDATE))

    6. Create V11__create_fee_record_table.sql migration:
       - Table: fee_record
       - Columns matching the FeeRecord entity exactly (snake_case columns)
       - NOT NULL constraints: id, fee_type, amount, due_date, status, owner_type, owner_id, dept_id, created_by
       - DEFAULT values: status='pending', source='manual'
       - Indexes:
         - INDEX idx_owner (owner_type, owner_id) -- supports Arc polymorphic queries
         - INDEX idx_status (status) -- filter by status
         - INDEX idx_due_date (due_date) -- date range queries, alert scans
         - INDEX idx_dept_id (dept_id) -- data isolation
         - INDEX idx_funding_source (funding_source) -- stats dimension
       - UNIQUE INDEX idx_unique_fee (owner_type, owner_id, fee_type, due_date) -- prevents duplicate fee records per D-13 anti-duplication
       - ENGINE=InnoDB CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
  </action>
  <read_first>
    - achievement-module/pom.xml (existing module list to add to)
    - achievement-module/achievement-patent/pom.xml (template for new module pom)
    - achievement-module/achievement-patent/src/main/resources/db/migration/V5__create_patent_table.sql (migration SQL pattern)
    - achievement-module/achievement-patent/src/main/java/com/institute/achievement/patent/entity/Patent.java (entity pattern)
    - (Plus FeeTypeEnum/FeeStatusEnum — new files, no existing to read)
  </read_first>
  <acceptance_criteria>
    - achievement-module/pom.xml contains `<module>achievement-fee</module>`
    - achievement-fee/pom.xml exists with correct parent, artifactId, and dependencies
    - FeeTypeEnum.java compiles with ANNUAL_FEE, REGISTRATION_FEE, MAINTENANCE_FEE, OTHER values
    - FeeStatusEnum.java compiles with PENDING, PAID, PAUSED values
    - FeeRecord.java compiles with all D-13 fields including ownerType/ownerId, deptId, fundSource
    - V11__create_fee_record_table.sql exists with all columns and indexes matching entity fields
    - V11 creates UNIQUE INDEX idx_unique_fee on (owner_type, owner_id, fee_type, due_date)
  </acceptance_criteria>
  <verify>
    <automated>cd d:/科研成果与知识产权管理系统 && mvn compile -pl achievement-module/achievement-fee -am -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    achievement-fee module compiles, fee_record migration SQL exists with correct schema and unique constraint
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement FeeRecord backend (Mapper, Service, Controller) with CRUD and paginated filtered listing</name>
  <files>
    - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/dto/FeeRecordDTO.java
    - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/dto/FeeRecordVO.java
    - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/mapper/FeeRecordMapper.java
    - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/service/FeeRecordService.java
    - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/service/impl/FeeRecordServiceImpl.java
    - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/controller/FeeRecordController.java
    - achievement-module/achievement-fee/src/main/java/com/institute/achievement/fee/scheduler/FirstFeeGenerationListener.java
  </files>
  <action>
    Implement the full fee record backend layer:

    1. Create FeeRecordDTO (input DTO) with jakarta.validation @NotBlank/@NotNull on required fields:
       - feeType (String, @NotBlank)
       - amount (BigDecimal, @NotNull)
       - dueDate (LocalDate, @NotNull)
       - ownerType (String, @NotBlank)
       - ownerId (Long, @NotNull)
       - fundingSource (String, optional)
       - source (String, default "manual")
       - deptId (Long, optional — defaults from SecurityUtils)

    2. Create FeeRecordVO (output VO) with all FeeRecord fields plus:
       - ownerName (String, @Transient — the patent/copyright name for display)
       - ownerInfo (Map&lt;String, Object&gt; for flexible display: patentName, applicationNo, etc.)
       - alertLevel (String, @Transient — will be populated by alert engine in 02-03)
       - statusLabel (String, @Transient — Chinese label)
       - feeTypeLabel (String, @Transient — Chinese label)

    3. Create FeeRecordMapper extending BaseMapper<FeeRecord> with:
       - Page<FeeRecordVO> selectFeeRecordPage(Page<?> page, @Param("query") FeeRecordQueryDTO query) — custom XML in mapper XML or @Select with &lt;script&gt;
       - The query supports filters: status, feeType, dueDateFrom, dueDateTo, keyword, deptId, ownerType, fundingSource
       - JOINs with patent table (when owner_type='patent') to get patent_name as owner_name
       - JOINs with copyright table (when owner_type='copyright') to get copyright_name as owner_name
       - Uses CASE WHEN to populate ownerName from the correct table based on owner_type
       - ORDER BY due_date ASC by default, or created_time DESC

    4. Create FeeRecordService interface with methods:
       - create(FeeRecordDTO) -> Long
       - update(Long id, FeeRecordDTO) -> void
       - getById(Long id) -> FeeRecordVO
       - page(int page, int size, FeeRecordQueryDTO) -> Page<FeeRecordVO>
       - delete(Long id) -> void

    5. Create FeeRecordServiceImpl implementing FeeRecordService:
       - create(): Sets deptId from SecurityUtils.getCurrentDeptId(), createdBy, sets source to dto.source or "manual"
       - update(): Uses UpdateWrapper (not LambdaUpdateWrapper per Phase 01-03 decision). Only updates editable fields: amount, fundingSource, paidAmount, voucherNo, status. Does NOT update dueDate, ownerType, ownerId (system-locked per D-17).
       - getById(): Maps entity to VO, loads ownerName via separate service call or JOIN
       - page(): Delegates to FeeRecordMapper.selectFeeRecordPage()
       - delete(): Checks status is PAUSED before allowing delete (cannot delete paid or pending records)

    6. Create FeeRecordController at @RequestMapping("/api/fees"):
       - POST / — createFeeRecord(@Valid @RequestBody FeeRecordDTO)
       - PUT /{id} — updateFeeRecord(@PathVariable Long id, @Valid @RequestBody FeeRecordDTO)
       - GET /{id} — getFeeRecord(@PathVariable Long id)
       - DELETE /{id} — deleteFeeRecord(@PathVariable Long id) — only allowed for paused records
       - GET /page — pageFeeRecords(@RequestParam int page, @RequestParam int size, @RequestParam(required=false) String status, @RequestParam(required=false) String feeType, @RequestParam(required=false) String fundingSource, @RequestParam(required=false) String keyword, @RequestParam(required=false) String dueDateFrom, @RequestParam(required=false) String dueDateTo, @RequestParam(required=false) String ownerType) — returns Page<FeeRecordVO>
       - All endpoints return Result<T>

    7. Create FirstFeeGenerationListener — Spring @EventListener for patent archived events:
       - Listens for AchievementArchivedEvent (custom event class in achievement-common, package com.institute.achievement.common.event)
       - Event fields: ownerType (String), ownerId (Long), achievementStatus (String)
       - The achievement-service (patent/copyright) publishes this event when an achievement transitions to ARCHIVED status
       - When a patent is archived (status transition to ARCHIVED), if patent.nextFeeDate != null, auto-create a fee_record with:
         - feeType = "annual_fee"
         - amount = 0 (placeholder, user edits later per D-17 and research Q1 decision)
         - dueDate = patent.nextFeeDate
         - ownerType = "patent"
         - ownerId = patent.id
         - status = "pending"
         - source = "auto_generated"
         - deptId = patent.deptId
       - Uses @Transactional(propagation = Propagation.REQUIRES_NEW) to run in its own transaction
       - This is the "first fee" creation per D-11 bullet 1 (auto-generate from nextFeeDate)

    8. Create FeeRecordQueryDTO inner class or separate file in dto/:
       - status, feeType, fundingSource, keyword, dueDateFrom, dueDateTo, ownerType, deptId (Long)
  </action>
  <read_first>
    - achievement-module/achievement-patent/src/main/java/com/institute/achievement/patent/controller/PatentController.java (controller pattern: @Slf4j @RestController @RequestMapping @RequiredArgsConstructor)
    - achievement-module/achievement-patent/src/main/java/com/institute/achievement/patent/service/PatentService.java (service pattern: @Slf4j @Service @RequiredArgsConstructor @Transactional)
    - achievement-module/achievement-patent/src/main/java/com/institute/achievement/patent/dto/PatentDTO.java (DTO pattern)
    - achievement-module/achievement-patent/src/main/java/com/institute/achievement/patent/dto/PatentVO.java (VO pattern)
    - achievement-module/achievement-patent/src/main/java/com/institute/achievement/patent/mapper/PatentMapper.java (mapper pattern)
    - The FeeRecord entity, FeeTypeEnum, FeeStatusEnum created in Task 1
  </read_first>
  <acceptance_criteria>
    - FeeRecordController POST /api/fees returns 200 with fee_record id
    - FeeRecordController GET /api/fees/page returns paginated results with ownerName populated from patent/copyright JOIN
    - FeeRecordController PUT /api/fees/{id} updates amount and fundingSource, but ignores dueDate changes
    - FeeRecordController DELETE /api/fees/{id} returns error for pending/paid records, succeeds for paused
    - FirstFeeGenerationListener creates fee_record when patent is archived
    - All endpoints use Result.success/Result.error wrapper
  </acceptance_criteria>
  <verify>
    <automated>cd d:/科研成果与知识产权管理系统 && mvn compile -pl achievement-module/achievement-fee -am -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    FeeRecord backend compiles, all CRUD endpoints respond correctly, paginated listing with JOIN-based ownerName works
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement frontend fee ledger views (FeeManagement wrapper, FeeLedger list, FeeDetail, router, API)</name>
  <files>
    - achievement-web/src/router/index.ts
    - achievement-web/src/views/fee/FeeManagement.vue
    - achievement-web/src/views/fee/FeeLedger.vue
    - achievement-web/src/views/fee/FeeDetail.vue
    - achievement-web/src/api/fee/feeRecord.ts
    - achievement-web/src/api/fee/index.ts
  </files>
  <action>
    Implement the frontend layer for fee ledger:

    1. Update router/index.ts to add the /fee route (per D-02):
       - Top level route with path '/fee', redirect '/fee/ledger'
       - Meta: title '费用管理', icon 'Money' (or 'Coin' — check available @element-plus/icons-vue)
       - Children:
         - path 'ledger' -> FeeLedger.vue, meta title '费用台账'
         - path 'plan' -> FeePlan.vue, meta title '缴费计划' (component placeholder for now)
         - path 'stats' -> FeeStats.vue, meta title '费用统计' (component placeholder for now)
         - path 'detail/:id' -> FeeDetail.vue, meta title '费用详情', hidden: true

    2. Create FeeManagement.vue wrapper (following AchievementList pattern):
       - Template: `<div class="fee-management"><router-view /></div>`
       - No extra wrapping — the sub-tab layout is handled at the FeeLedger/FeePlan/FeeStats level
       - Export default component definition

    3. Create feeRecord.ts API service in src/api/fee/:
       ```
       export function getPage(params): GET /api/fees/page { params: page, size, status, feeType, fundingSource, keyword, dueDateFrom, dueDateTo, ownerType }
       export function getById(id): GET /api/fees/{id}
       export function create(data): POST /api/fees
       export function update(id, data): PUT /api/fees/{id}
       export function remove(id): DELETE /api/fees/{id}
       ```
       - API response types: FeeRecordVO interface matching backend fields
       - Import http from '@/api/index' (existing axios wrapper)

    4. Create fee/index.ts barrel export:
       - Export * from './feeRecord'

    5. Create FeeLedger.vue (main fee record list tab — per D-03, D-04):
       - Top section: Quick filter tags row (D-04):
         - "全部" (default), "即将逾期" (pending + dueDate within 30 days), "已逾期" (pending + dueDate < today), "本月需缴费" (pending + dueDate within current month)
         - Each tag calls fetchList with appropriate params
       - Filter row below quick tags:
         - el-select for feeType (options from FeeTypeEnum labels)
         - el-select for fundingSource (data loaded from dict API)
         - el-select for status (待缴费/已缴费/已暂停)
         - el-date-picker for due date range (dueDateFrom - dueDateTo)
         - keyword input (searches in patent name / copyright name)
         - "搜索" button, "重置" button
         - "高级筛选" toggle button that expands to show ownerType select (专利/软著)
       - el-table with columns (D-03):
         - Selection checkbox column (for batch pay in 02-04 — add now with v-model)
         - 序号 (index)
         - 费用类型 (feeTypeLabel, using el-tag with type mapping: annual_fee=primary, registration_fee=success, maintenance_fee=warning, other=info)
         - 关联成果 (ownerName with link to patent/copyright detail)
         - 截止日期 (dueDate, colored based on alert status — logic placeholder for now)
         - 金额 (amount, formatted ¥xxx.xx)
         - 实缴金额 (paidAmount)
         - 缴费日期 (paidDate)
         - 经费来源 (fundingSourceLabel from dict)
         - 状态 (statusLabel, el-tag: pending=warning, paid=success, paused=info)
         - 操作 (查看 编辑 删除) — per D-05, no batch operations here
       - el-pagination at bottom
       - Empty state (el-empty) when no records
       - "查看" opens FeeDetail in same tab or el-dialog
       - "编辑" opens inline el-dialog with editable fields (amount, fundingSource, paidAmount, voucherNo, status). dueDate is readonly (D-17).
       - "删除" only available when status='paused'
       - Filter state preserved in reactive refs

    6. Create FeeDetail.vue (per D-06):
       - el-tabs layout:
         - tab "基本信息": all fee record fields in read-only label-value pairs
         - tab "缴费历史": placeholder for now (future use)
         - tab "附件": embed AttachmentUploader component (reused from Phase 01) for payment voucher uploads (D-09)
         - tab "操作日志": placeholder for audit log integration (future use)
       - Back button to return to ledger list
       - Receives fee record ID from route params, loads via getById()
  </action>
  <read_first>
    - achievement-web/src/router/index.ts (existing route definitions to extend)
    - achievement-web/src/views/achievement/AchievementList.vue (table + filter pattern for FeeLedger)
    - achievement-web/src/views/achievement/AchievementDetail.vue (detail tab layout for FeeDetail)
    - achievement-web/src/api/achievement/patent.ts (API service pattern for feeRecord.ts)
    - achievement-web/src/api/index.ts (existing axios http wrapper)
  </read_first>
  <acceptance_criteria>
    - /fee route redirects to /fee/ledger showing FeeLedger component
    - Fee ledger shows paginated el-table with selection checkboxes, 9 data columns, action buttons
    - Quick filter tags (全部/即将逾期/已逾期/本月需缴费) all work, triggering filtered API calls
    - Advanced filter section expands/collapses with toggle button
    - "查看" opens FeeDetail with 4 tabs, "附件" tab has AttachmentUploader
    - "编辑" dialog shows editable amount/fundingSource, readonly dueDate
    - "删除" button is disabled for pending/paid records, enabled for paused
    - FeePlan and FeeStats routes show placeholder content
  </acceptance_criteria>
  <verify>
    <automated>cd d:/科研成果与知识产权管理系统/achievement-web && npx vue-tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    Fee ledger frontend compiles, all filter/search/pagination interactions work end-to-end
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Unauthenticated/unauthorized HTTP requests to /api/fees endpoints |
| intra-module | Fee module reads patent data via Arc join (no direct module dependency) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Spoofing | POST/PUT /api/fees | mitigate | @PreAuthorize on controller class or methods (requires fee:manage permission); SecurityUtils.getCurrentUserId() injects createdBy/updatedBy |
| T-02-01-02 | Tampering | PUT /api/fees/{id} | mitigate | Only whitelisted fields editable (amount, fundingSource, paidAmount, voucherNo, status). Use MapStruct or manual field copy — never accept DTO directly to entity |
| T-02-01-03 | Information Disclosure | GET /api/fees/page | mitigate | SQL-layer data isolation: dept_id automatically injected by MyBatis-Plus interceptor from SecurityUtils.getCurrentDeptId() |
| T-02-01-04 | Denial of Service | GET /api/fees/page (large offset) | accept | Fee records < 100K per year per dept. Pagination via Page helper prevents full table scan. No rate limiting at this scale |
| T-02-01-05 | Elevation of Privilege | DELETE /api/fees/{id} | mitigate | Only the creator (created_by == currentUser) can delete, AND only when status='paused'. Backend checks both conditions |
| T-02-01-SC | Tampering | npm/maven dependency installs | mitigate | No new external packages. All Maven dependencies are existing project libs verified through Phase 0/1 |
</threat_model>

<verification>
1. `mvn compile -pl achievement-module/achievement-fee -am` succeeds
2. `npx vue-tsc --noEmit` passes in achievement-web
3. V11 migration SQL creates fee_record table with all columns and indexes
4. /api/fees/page returns correct paginated results with ownerName populated
5. Fee ledger frontend renders with correct columns, filters, quick tags
</verification>

<success_criteria>
1. New `achievement-fee` sub-module compiles and is registered in module pom
2. `fee_record` table exists in database (or migration SQL ready) with all D-13 fields
3. FeeRecordController provides full CRUD + paginated listing with multi-field filtering + JOIN-based ownerName
4. First fee auto-generated from patent.nextFeeDate on patent archive event via @EventListener
5. /fee route renders FeeManagement wrapper with 3 tab slots active
6. FeeLedger.vue shows el-table with quick filters, advanced filter toggle, paginated data, selection checkboxes
7. FeeDetail.vue renders 4 tabs including AttachmentUploader for vouchers
8. Edit dialog allows amount/fundingSource changes but locks dueDate (D-17)
9. Delete only works for paused records (D-05)
</success_criteria>

<output>
Create `.planning/phases/02-fee-management-alerts/02-01-SUMMARY.md` when done
</output>
